# iflow.ai lab平台 — 完整方案 v3

> 基准会议：集成电路AI实训平台项目部署会议 2026年7月29日
> 日期：2026-07-31
> 已有基础：ic_agent_os (adapter/composer/state) / iFlow (RTL + 脚本) / iEDA (物理设计 + iSTA + iDRC)

---

## 一、EDA 工具选型（全链路）



### 1.1 全链路工具表

| # | 步骤 | 工具 | 备注 |
|---|------|------|------|
| 1 | RTL 语法检查 | **Verible** | Google 开源 SystemVerilog linter |
| 2 | 功能仿真（ChipMATE 验证） | **ChipMATE + Icarus Verilog** (iverilog+vvp) | ChipMATE自动生成测试，Icarus执行RTL仿真 |
| 3 | 高速仿真（回归/大设计） | **Verilator** | 会议明确要求；比 Icarus 快 10-100× |
| 4 | 波形查看（Web） | **Surfer** (浏览器模式) / **vcdrom** (纯 JS) | 前端需要 Web 波形组件 |
| 5 | 逻辑综合 + 可综合性检查 | **Yosys** | ✅ 已集成；iEDA 无综合引擎。**注意**：Verilator 是仿真编译器，不检查可综合性，必须用 Yosys 确认 |
| 6 | 形式验证（SVA property 证明） | **SymbiYosys** (sby) | BMC / k-induction / cover。**仅能证明已写好的 SVA property，不能自动检测 CDC/RDC** |
| 7 | STA | **iSTA**（iEDA 内置） | ✅ 已集成（`ieda_py` in-process 调用）；主要负责STA分析,可在指定约束下分析多Clock,多Mode,不提供完整UPF Power Intent一致性验证 |
| 8 | 物理设计 | **iEDA** | ✅ 已集成（subprocess 隔离模式） |
| 9 | 寄生抽取 | **iEDA 内置** | iEDA 内部处理，与物理设计一体 |
| 10 | DRC | **iDRC**（iEDA 内置） | iEDA post-route DRC |
| 11 | LVS | **Netgen** | 开源 LVS 标准工具 |
| 12 | GDS 生成 | **iEDA 内置** | iEDA 流程末端输出 |
| 13 | DFT/ATPG | **Yosys `dft`** | 仅基础 scan insertion；**开源无成熟 ATPG**；流片需商业 DFT 工具 |

**开源工具链的已知能力边界与处理策略**（实训平台如实标注，避免误导学生）：

| 缺失能力 | 开源现状 | 是否阻塞主流程 | 对下游的影响 | 平台处理策略 | 策略详情 |
|---------|---------|:---:|------------|:---:|---------|
| **CDC 检查** | 无成熟开源工具 | 否 | **STA 可能受影响**：多时钟设计若无 clock groups 约束，iSTA 会在异步跨域路径上报告虚假违例。Agent 需能区分"真违例"和"跨域噪声"。GDS 若不经同步可能硬件亚稳态 | **教育替代** | Agent 自动标注跨域路径 → 引导学生手写同步器 → SymbiYosys 逐条验证同步器 property。CDC 本身是重要教学内容，手工分析比黑盒工具更有教育价值 |
| **RDC 检查** | 同上 | 否 | 同上（学生设计极少多复位域，触发频率极低） | **教育替代** | 同 CDC 模式。学生在实际项目中几乎不会遇到多复位域设计 |
| **UPF 完整验证** | 无（iSTA 仅能按指定约束做多电压域时序分析） | 否 | 无 UPF → 零影响。有 UPF → iEDA 仍会按 UPF 指令插 Level Shifter/Isolation，但若 UPF 本身有错，物理实现按错误 power intent 跑，产出有隐患的 GDS | **诚实标注** | UPF 语法解析（平台做）→ 基础检查通过 → 完整一致性验证标注为"需商业工具(VC LP/CLP)"。不阻塞流程，但向学生说明风险 |
| **DFT (完整)** | Yosys `dft` 仅基础 scan insertion | 否 | **零影响**。DFT 是独立分支（产出 scan 网表），不插入反而减少绕线约束。仅影响可测性，不影响功能 | **诚实标注** | Yosys 做基础 scan insertion → MBIST/LBIST/BSCAN 标注为"需商业工具(Tessent/TestMAX/Modus)"。学生需理解"如果没有 DFT，芯片造出来也无法测试" |
| **ATPG** | 完全无 | 否 | **零影响**。ATPG 是流程叶子节点——消费 DFT 网表，产出测试向量，不被任何下游步骤消费 | **诚实标注** | 标记为"需商业工具"。提供教育说明：ATPG 是什么、为什么需要、测试覆盖率如何计算 |
| **可综合性检查** | 无独立工具（Yosys `synth` 综合过程中可暴露） | 否 | 若用 Verilator 仿真通过但不可综合的代码未被发现，Yosys 综合阶段会报错，此时需回头修改 RTL | **无需特殊处理** | Yosys `synth` 本身就能暴露不可综合代码——综合失败就是最好的可综合性检查。Agent 在综合阶段报错时引导学生修改 RTL |

**三种处理策略的定义**：

| 策略 | 适用条件 | 平台做什么 | 不做什么 |
|------|---------|-----------|---------|
| **教育替代** | 缺失的能力本身是重要教学内容，且手工分析过程有教育意义 | Agent 引导分析 + 提供模板 + 工具辅助验证 | 不假装自动检测通过 |
| **诚实标注** | 缺失的能力没有教育替代价值（如"手工生成 ATPG 向量"无意义） | 标注能力为 `placeholder` → 平台执行基础步骤 → 余下显示"需商业工具" + 教育说明链接 | 不阻塞流程，不编造假结果 |
| **无需特殊处理** | 开源工具链的现有步骤能自然覆盖 | 依赖现有步骤的报错来发现和修正问题 | — |

**关键原则**：实训平台的目标是**教育**，不是替代商业工具。所有缺失能力都不阻塞 RTL→GDS 主流程（因为它们是并行质量检查或独立分支或叶子节点）。平台选择"教育替代"还是"诚实标注"，取决于该能力是否有手工教学价值。

### 1.2 iEDA 物理设计流程

iEDA 的物理设计步骤（替代 OpenROAD 12 步）：

```
floorplan → fixFanout → placement → CTS → optDrv → optHold → legalization → routing → filler
   (iFP)      (iNO)       (iPL)    (iCTS)   (iTO)    (iTO)       (iPL)      (iRT)     (iPL)
```

| 步骤 | iEDA 模块 | 对应功能 |
|------|----------|---------|
| floorplan | iFP | Die/Core 规划、Macro/IO 摆放 |
| fixFanout | iNO | 高扇出网络修复 |
| placement | iPL | 标准单元全局布局 |
| CTS | iCTS | 时钟树综合 |
| optDrv | iTO | 驱动强度优化 |
| optHold | iTO | Hold 时序修复 |
| legalization | iPL | 单元合法化 |
| routing | iRT | 全局 + 详细布线 |
| filler | iPL | Filler/Decap 插入 |

配套分析工具：iSTA（STA）、iDRC（DRC）、iIR（IR-drop）、iPNP（电源网络）。

### 1.3 Adapter 就绪状态

```
现有代码                       实训平台
─────────────────────────────────────────
Yosys ✅ digital_runner.py    维持（综合）
iEDA  ✅ ieda_runner.py       维持（物理设计 + iSTA/时序 + iDRC/物理验证）
Verible ❌                    新增 adapter
Icarus  ❌                    新增 adapter
Verilator ❌                   新增 adapter
SymbiYosys ❌                  新增 adapter
Netgen   ❌                    新增 adapter
```

### 1.4 关键决策说明

**为什么仿真同时选 Icarus + Verilator？**

- Icarus：ChipMATE 交叉验证的硬依赖；支持更完整 SystemVerilog testbench 语法
- Verilator：会议明确要求；C 模型高速仿真，适合大设计回归
- Agent 根据场景自动选择：验证 RTL 生成结果 → Icarus；回归测试 → Verilator

**为什么用 Surfer 而不是 GTKWave？**

GTKWave 是 GTK 桌面应用无法嵌入 Web。Surfer 支持浏览器模式（Rust WASM），vcdrom 是纯 JS VCD 解析器。轻量波形用 vcdrom 即时渲染，复杂波形用 Surfer。

---

## 二、AI 模型选型


### 2.1 角色与模型分配

| 角色 | 推荐模型 | 备选 | 部署位置 | 选择理由 |
|------|---------|------|---------|---------|
| **RTL 生成 (Verilog Agent)** | ChipMATE-V-9B | ChipMATE-V-4B | GPU (vLLM) | ChipMATE 是目前最强的开源 RTL 多 Agent 框架；9B 是最大规格 |
| **RTL 生成 (Python Agent)** | ChipMATE-P-9B | ChipMATE-P-4B | GPU (vLLM) | 与 V Agent 配对做交叉验证 |
| **Agent 决策** | **DeepSeek-V4** API | Qwen3-72B (GGUF, CPU) / Qwen3-235B (GGUF, CPU) | API / 本地 CPU | DeepSeek-V4 是最新最强；API 便宜 ($0.27/M in)；可本地跑 Qwen3-235B 做离线备选 |
| **教育交互** | **DeepSeek-V4** API | Qwen3-72B (GGUF, CPU) | API / 本地 CPU | 同上；教育对话对延迟不敏感，CPU 推理可接受 |

### 2.2 ChipMATE 集成方式

```
用户自然语言 → ChipMATE 框架
                  ├── Verilog Agent (生成 RTL 实现)
                  ├── Python Ref Agent (生成参考模型)
                  ├── Icarus Verilog (交叉验证)
                  └── 输出：验证通过的 RTL + 匹配率报告
```

ChipMATE 支持两种后端模式：
- **本地 vLLM 模式**：加载开源模型权重，零 API 成本
- **OpenAI 兼容 API 模式**：对接 DeepSeek API 或其他兼容端点

---

## 三、前端页面设计

### 3.0 技术栈

| 层 | 选型 | 理由 |
|----|------|------|
| 前端框架 | **Next.js 14 + React 18 + TypeScript** | 生态成熟；代码编辑器、图表库丰富 |
| 代码编辑器 | **Monaco Editor**（VS Code 内核） | Verilog 语法高亮、自动补全 |
| 轻量波形 | **vcdrom**（纯 JS VCD 解析） | 嵌入 React；<1000 信号切换够用 |
| 深度波形 | **Surfer** WASM 模式 | 支持大 VCD/FST；缩放、光标、分组 |
| 版图/GDS 预览 | **KLayout CLI → PNG 瓦片 → OpenSeaDragon** | 可缩放地图式浏览 |
| 图表 (收敛历史等) | **Recharts** | React 原生，轻量 |
| 实时通信 | **WebSocket** (FastAPI 后端) | Flow 步骤实时推送 |
| 后端 API | **FastAPI** (Python) | 与 ic_agent_os 同语言；WebSocket 原生支持 |
| 用户认证 | **JWT** + SQLite | 多用户隔离 |

### 3.1 总体布局

```
┌──────────────────────────────────────────────────────────────┐
│  [Logo] 集成电路AI实训平台    [阶段导航]    [用户] [⚙] [?]    │
├────────┬───────────────────────────────────┬─────────────────┤
│        │                                   │                 │
│  📐     │       中央主工作区                  │  右侧辅助面板    │
│ 阶段1  │       (动态切换内容)                │                 │
│ RTL    │                                   │  📖 知识卡片    │
│ 设计   │                                   │  🏆 通关进度    │
│        │                                   │  🤖 Agent 反馈  │
│ 🔍     │                                   │  📜 操作历史    │
│ 阶段2  │                                   │                 │
│ 仿真   │                                   │                 │
│ 验证   │                                   │                 │
│        │                                   │                 │
│ ⚡     │                                   │                 │
│ 阶段3  │                                   │                 │
│ 芯片   │                                   │                 │
│ 实现   │                                   │                 │
│        │                                   │                 │
│ 📊     │                                   │                 │
│ 对比   │                                   │                 │
│ 实验   │                                   │                 │
│        │                                   │                 │
├────────┴───────────────────────────────────┴─────────────────┤
│ 🟢 Agent 就绪 │ 当前: 阶段1-关卡2 │ 已用时 03:24 │            │
└──────────────────────────────────────────────────────────────┘
```

### 3.2 阶段 1：RTL 设计与生成

**定位**：用户在此完成 RTL 的编写/生成、编译、基础仿真和波形查看。

三 Tab 模式切换：

- **🤖 AI 生成**：需求输入框 + ChipMATE Agent 实时反馈面板 + 代码编辑器只读 + 交叉验证进度条
- **✍ 手写**：Monaco 代码编辑器可编辑 + 右侧 Verilog 语言参考卡片 + 一键编译
- **📎 上传**：拖拽上传区域 + 已有文件树浏览 + 自动识别顶层 module

**AI 生成模式关键交互**：

```
用户输入："设计一个带异步复位和使能的 4 位加法计数器"

Agent 反馈面板实时显示：
  Round 1: 生成 RTL + Python 参考模型 → 交叉验证匹配率 33% ❌ → 重试
  Round 2: 调整后匹配率 67% ⚠️ → 继续优化
  Round 3: 匹配率 100% ✅ → 输出最终 RTL

每轮显示：Agent 做了什么、为什么匹配失败、如何修正
```

**波形面板**（仿真后出现）：vcdrom/Surfer 嵌入，显示 clk / rst_n / q[3:0] / overflow，支持缩放和光标测量。

**通关条件**（铜/银/金三级）：
- 🥉 Verilator 编译无 Error
- 🥈 Icarus 仿真波形与时序图一致
- 🥇 无 Latch 推断 + Verible Lint 通过 + Style Check 通过

### 3.3 阶段 2：仿真与验证

**定位**：对 RTL 进行三维度验证——代码 Debug、动态功能仿真、静态形式化验证。对齐会议要求的三个验证维度。

| 会议要求 | 前端入口 | 后端工具 |
|---------|---------|---------|
| 代码 debug 查错 | Bug 追踪面板 + 源码-波形联动 | Agent 分析 log |
| 动态功能仿真 | 仿真控制 + Testbench 编辑器 + 覆盖率 | Icarus / Verilator |
| 静态形式化验证 | SVA 属性编辑器 + SymbiYosys 接口 | sby (BMC/k-induction) |

**关键组件**：
- **仿真控制面板**：时间范围、断点设置、步进控制
- **Testbench 编辑器**：Monaco 编辑器 + AI 生成 testbench 按钮
- **Bug 追踪面板**：Agent 从仿真 log 中提取错误 → 定位到源码行 → 分类（时序/逻辑/语法）
- **覆盖率热力图**：Line / Branch / FSM / Toggle 四项覆盖率，Matrix 可视化
- **形式化验证**：SVA 编辑器 + 属性模板引导（Agent 提示常用断言模式）

### 3.4 阶段 3：芯片实现

**定位**：从 RTL 到 GDS 的完整物理实现。核心不是跑固定步骤，而是 Agent 动态生成 Solution，用户观察收敛过程。

**iEDA 物理设计流程映射**：

```
用户 RTL → Yosys 综合 → 门级网表 ──→ iEDA 物理设计
                                       ├── floorplan (iFP)
                                       ├── fixFanout (iNO)
                                       ├── placement (iPL)
                                       ├── CTS (iCTS)
                                       ├── optDrv (iTO)
                                       ├── optHold (iTO)
                                       ├── legalization (iPL)
                                       ├── routing (iRT)
                                       ├── filler (iPL)
                                       ├── iSTA (时序)
                                       ├── iDRC (物理验证)
                                       └── GDS 导出
```

**关键组件**：
- **Solution 配置面板**：场景选择（体验/课程/竞赛/科研/流片）→ Agent 自动裁剪 Flow
- **Flow 实时可视化**：每步状态（✅通过 / 🔄运行中 / ⏳待执行 / ❌失败 + 原因），支持暂停/继续/重置
- **实时指标面板**：WNS / TNS / 面积 / 功耗 / DRC 违例数，随执行更新
- **GDS 预览**：KLayout 服务端渲染 → PNG 瓦片 → OpenSeaDragon 可缩放地图
- **收敛历史图**（核心教育价值）：Recharts 折线图展示多轮迭代中 WNS/面积/DRC 的变化趋势
  ```
  WNS  ▲                   面积
  0.2 ┤   ┌──             0.05┤████
    0 ┤──┘  ├──            0.04┤
  -0.2┤    │   └──         0.03┤
      R1   R2   R3            R1 R2 R3
  ```
- **详细报告 Tab**：时序 / 面积 / 功耗 / DRC / LVS / 原始 Log

**Agent 在阶段 3 的三层职责**：

| 层 | 职责 | 前端表现 |
|----|------|---------|
| L1: Flow 选择 | 根据场景 + 设计特征裁剪步骤 | 左侧 Flow 列表动态变化 |
| L2: 阶段内迭代 | 单步不通过 → 调参数 → 重跑本步 | 某步出现 🔄→✅ 循环 + Agent 说明 |
| L3: 跨阶段回退 | 下游反复失败 → 退回上游换策略 | Flow 列表出现 ↩ 箭头 + Agent 解释原因 |

### 3.5 右侧辅助面板（全阶段共用）

固定显示四个区域：
1. **📖 知识卡片**：根据当前操作上下文自动切换（如："什么是 Setup Time？WNS < 0 意味着什么？"）
2. **🏆 通关进度**：三阶段 × 铜银金三级 = 9 个徽章位 + 解锁状态
3. **🤖 Agent 反馈流**：实时流式输出 Agent 的决策和解释（"resize 第 2 轮：density 降低 5%，WNS 从 -0.3 → +0.1"）
4. **📜 操作历史**：时间戳 + 操作摘要，可点击回看历史结果

---

## 四、部署架构（详细解释）

### 4.1 为什么用 Docker + 多容器？

**核心问题**：EDA 工具依赖复杂（特定版本的系统库、环境变量、License 配置），直接安装在宿主机上会互相冲突。用户代码在 EDA 工具中运行，需要隔离以防止恶意代码影响宿主机。

**解决方案**：将不同类型的工具放在独立 Docker 容器中。每个容器 = 一个隔离的小环境，只包含一类工具及其依赖。

**容器划分逻辑**（按功能分组，而非每种工具一个容器）：

```
为什么这样分组？

仿真容器（Icarus+Verilator+Verible+Surfer）
→ 这些工具都是"读 RTL，出波形/报告"，共享同一种工作模式
→ 仿真时不需要综合工具，综合时不需要仿真工具
→ 分在不同容器 = 可以同时仿真和综合，互不影响

综合容器（Yosys+SymbiYosys）
→ 综合和形式验证共享 Yosys 内核
→ 放在一起减少重复依赖

物理+验证容器（iEDA+Netgen）
→ iEDA 包含物理设计全流程（floorplan→GDS）+ iSTA + iDRC
→ Netgen 做最终 LVS
→ 放在一起因为物理验证依赖物理设计输出
```

**容器间如何协作？**

容器之间不直接通信。它们通过**共享文件目录**交换数据：

```
/api/workspace/user_001/
    ├── rtl/          ← 用户上传或 AI 生成的 RTL
    ├── constraint/   ← SDC / UPF 等约束文件
    ├── netlist/      ← Yosys 综合产物（门级网表）
    ├── reports/      ← 所有工具的 log、报告、波形 VCD
    └── gds/          ← iEDA 最终输出 GDS
```

一个完整流程的数据流：

```
用户上传 RTL → 存入 workspace/rtl/
  → API 通知仿真容器："跑 Verible lint"
  → 仿真容器读 workspace/rtl/，写 lint 报告到 workspace/reports/
  → API 通知综合容器："跑 Yosys"
  → 综合容器读 workspace/rtl/，写网表到 workspace/netlist/
  → API 通知物理容器："跑 iEDA"
  → 物理容器读 workspace/netlist/，写 GDS 到 workspace/gds/
```

API 容器是"大脑"，其他容器是"手"。手之间不直接对话，通过大脑（API）协调，通过共享目录交换文件。

### 4.2 为什么需要 WebSocket？

**核心问题**：iEDA 跑一个完整物理设计可能要 10-20 分钟。如果前端用普通的 HTTP 请求（一问一答），用户在这 20 分钟里看到的是白屏或转圈——不知道进度、不知道卡在哪里、不知道是否已经崩溃。

**WebSocket 解决什么？**

HTTP 是"你问我才答"：前端发请求 → 后端处理完 → 一次性返回结果。

WebSocket 是"建立一个长连接，后端**主动**向前端推送消息"。

对应用户体验的差异：

```
HTTP 模式（不好）：
  用户点"开始" → 转圈 10 分钟 → 突然显示"完成"或"失败"
  中间 10 分钟：不知道发生了什么

WebSocket 模式（好）：
  用户点"开始" → 立即看到：
    "▶ floorplan 开始..."
    2 秒后 → "✅ floorplan 完成 (2.1s)"
    "▶ placement 开始..."
    20 秒后 → "✅ placement 完成 (19.4s)"
    "▶ routing 开始..."
    15 分钟后 → "✅ routing 完成 (892s)"
    "▶ iSTA 开始..."
    3 秒后 → "❌ iSTA 失败: WNS = -0.5ns"
    "↩ Agent 建议: 回到 placement, density 降低 5%"
    "▶ placement (第2轮) 开始..."
```

**技术实现**（概念层面）：

```
前端 (React)                    后端 (FastAPI)                  Docker 容器
     │                               │                              │
     │── 建立 WebSocket 连接 ────────→│                              │
     │                               │                              │
     │── {"action":"run_flow"} ─────→│                              │
     │                               │── docker exec ieda... ──────→│
     │                               │                              │
     │← {"type":"step_start", ───────│← stdout 出现 "Running iFP" ──│
     │    "step":"floorplan"}        │                              │
     │                               │                              │
     │← {"type":"log", ──────────────│← stdout 流式文本 ────────────│
     │    "text":"Init floorplan..."}  │                              │
     │                               │                              │
     │← {"type":"step_done", ────────│← iFP 进程退出 ───────────────│
     │    "step":"floorplan",        │                              │
     │    "duration":2.1}            │                              │
     │                               │                              │
     │← {"type":"step_start", ───────│── docker exec ieda (iPL)... →│
     │    "step":"placement"}        │                              │
```

WebSocket 承载三类消息：
1. **step_start / step_done**：每个步骤的启停 + 耗时
2. **log**：工具 stdout 的流式文本（用户可以看到详细日志）
3. **metric_update**：关键指标的实时更新（WNS 变化、DRC 违例数变化）

### 4.3 整体架构图

```
┌──────────────────────────────────────────────────────────────┐
│                        用户浏览器                              │
│                  https://platform.example.com                 │
└──────────────────────────┬───────────────────────────────────┘
                           │ HTTP + WebSocket
                           ▼
┌──────────────────────────────────────────────────────────────┐
│                     Nginx (反向代理)                           │
│   · 静态文件 (/assets/*) → 直接返回                           │
│   · API 请求 (/api/*) → 转发给 FastAPI                        │
│   · WebSocket (/ws/*) → 转发给 FastAPI                        │
└──────────────────────────┬───────────────────────────────────┘
                           │
              ┌────────────┼────────────┐
              ▼            ▼            ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ 前端容器       │ │ API 容器      │ │ AI 容器       │
│ Next.js      │ │ FastAPI      │ │ vLLM          │
│ :3000        │ │ :8000        │ │ :8001, :8002  │
│              │ │              │ │               │
│ 渲染页面      │ │ · 用户管理    │ │ · ChipMATE-V  │
│              │ │ · Flow 编排   │ │ · ChipMATE-P  │
│              │ │ · WebSocket   │ │ · Agent LLM   │
│              │ │ · 文件管理    │ │               │
└──────────────┘ └──────┬───────┘ └──────────────┘
                        │
                        │ docker exec (启动工具)
                        │
        ┌───────────────┼───────────────┐
        ▼               ▼               ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ 仿真容器       │ │ 综合容器       │ │ 物理+验证容器  │
│              │ │              │ │               │
│ · Icarus     │ │ · Yosys      │ │ · iEDA        │
│ · Verilator  │ │ · SymbiYosys │ │   (iFP/iPL/   │
│ · Verible    │ │              │ │    iCTS/iTO/  │
│ · Surfer     │ │              │ │    iRT/iSTA/  │
│              │ │              │ │    iDRC/iIR)   │
│              │ │              │ │ · Netgen      │
└──────────────┘ └──────────────┘ └──────────────┘
        │               │               │
        └───────────────┼───────────────┘
                        │ 共享数据卷
                        ▼
┌──────────────────────────────────────────────────────────────┐
│               /workspace/ (用户工作空间)                        │
│                                                              │
│   {user_id}/                                                 │
│     ├── rtl/           # 用户上传或 AI 生成的 RTL             │
│     ├── constraint/    # SDC / UPF 等约束文件                 │
│     ├── netlist/       # Yosys 综合产物（门级网表）            │
│     ├── reports/       # 所有工具的 log、报告、波形 VCD       │
│     └── gds/           # iEDA 最终输出 GDS                    │
└──────────────────────────────────────────────────────────────┘
```

---

## 五、对比实验功能设计

### 5.1 需求来源

参考 [aes11_detailed_comparison.md](/home/xu/ic_agent_os/aes11_detailed_comparison.md)，该文档展示了一个完整的对比实验：**同一设计 (AES) 在 4 种 PDK (sky130/nangate45/asap7/ics55) × 3 种利用率 (a/b/t = 35%/30%/25%) = 13 个配置下**的全流程对比。

对比维度包括：
- **横向总表**：Die / Cells / WNS / Fmax / Power / EGR WL / DRC / Routing Runtime / IR-drop
- **关键横向图**：柱状图对比 Die Area / Frequency / Power / DRC / Routing Runtime
- **空间 Map 横向对比**：密度图 / 拥塞图 / 布线溢出图 / DRC 密度图（统一色标，可横向对比）
- **同工艺纵向结论**：同一 PDK 下不同利用率的影响分析
- **各设计阶段纵向对比**：每设计 × 每阶段的指标表 + 版图截图 + 空间 Map

### 5.2 平台需要支持的对比实验能力

#### 5.2.1 对比实验的通用模型

一个对比实验 = **一个设计** × **N 个配置变量** × **M 个配置值**，跑完全部组合后横向对比。

```
实验示例：
  设计: AES
  变量1: PDK → [sky130, nangate45, asap7, ics55]
  变量2: 利用率 → [35%, 30%, 25%]
  变量3: 频率 → [100MHz, 200MHz]
  
  总组合数: 4 × 3 × 2 = 24 个任务
```

变量类型包括：

| 变量类型 | 示例 | 影响范围 |
|---------|------|---------|
| **工艺** (PDK) | sky130 / nangate45 / asap7 / ics55 | 全流程 |
| **物理参数** | 利用率 / Die面积 / 宽长比 | floorplan+ |
| **时序约束** | 目标频率 / Clock uncertainty / Input delay | 综合 + STA |
| **工具参数** | iRT 最大迭代次数 / CTS 目标 Skew | 对应步骤 |
| **设计版本** | RTL 优化版 / 原始版 | 全流程 |

#### 5.2.2 前端：对比实验配置页

作为左侧导航的**第四个入口**（阶段 1/2/3 + 对比实验），或阶段 3 内的高级功能。

```
┌──────────────────────────────────────────────────────────────┐
│  对比实验                                                      │
├────────────────────────────────┬─────────────────────────────┤
│                                │                             │
│  实验配置                       │  已配置的组合列表            │
│                                │                             │
│  设计: [AES ▼]  [上传其他RTL]   │  #1 sky130, 35%, 100MHz    │
│                                │  #2 sky130, 30%, 100MHz    │
│  ┌─ 变量 1 ─────────────────┐  │  #3 sky130, 25%, 100MHz    │
│  │ 类型: [工艺 ▼]            │  │  #4 nangate45, 35%, 100MHz │
│  │ 值:  ☑ sky130            │  │  #5 nangate45, 30%, 100MHz │
│  │      ☑ nangate45         │  │  #6 nangate45, 25%, 100MHz │
│  │      ☑ asap7             │  │  ...                       │
│  │      ☐ ics55             │  │                             │
│  └──────────────────────────┘  │  总计: 24 个任务             │
│                                │  预计耗时: ~6 小时           │
│  ┌─ 变量 2 ─────────────────┐  │                             │
│  │ 类型: [利用率 ▼]          │  │  [▶ 开始实验]               │
│  │ 值:  [35%] [30%] [25%]   │  │  [💾 保存配置]              │
│  └──────────────────────────┘  │                             │
│                                │                             │
│  [+ 添加变量]                   │                             │
│                                │                             │
├────────────────────────────────┴─────────────────────────────┤
│  实验进度 (开始后显示)                                          │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ #1 ████████████████████ ✅ 完成                           │ │
│  │ #2 ████████████████████ ✅ 完成                           │ │
│  │ #3 ████████████████░░░░ 🔄 运行中 (routing, 58%)         │ │
│  │ #4 ⏳ 排队中                                              │ │
│  │ ...                                                      │ │
│  └──────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

#### 5.2.3 前端：对比结果展示页

实验跑完后，自动生成对比报告。参考 aes11 报告的结构，分为四个展示区域：

**区域 A：横向总表**

```
┌──────────────────────────────────────────────────────────────┐
│  横向对比总表                                                  │
│  [导出 CSV] [导出 Excel] [选择列...]                           │
├──────────────────────────────────────────────────────────────┤
│ Design          │ PDK    │ Die  │ Cells │ WNS   │ Fmax │ ... │
│ aes_sky130_a    │ sky130 │ 588  │ 9434  │ -6.56 │ 110  │ ... │
│ aes_sky130_b    │ sky130 │ 629  │ 9434  │ -6.56 │ 110  │ ... │
│ aes_nangate45_a │ ng45   │ 267  │ 9961  │ +1.38 │ 893  │ ... │
│ ...             │ ...    │ ...  │ ...   │ ...   │ ...  │ ... │
└──────────────────────────────────────────────────────────────┘

表格特性：
- 列可排序（点击列头按该指标排序）
- 行可勾选（选择要纳入图表的设计）
- 单元格颜色编码（值越好越绿，越差越红）
```

**区域 B：关键横向图**

```
┌──────────────────────────────────────────────────────────────┐
│  关键横向图                                                    │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─ Die Area 柱状图 ─────────────────────────────────────┐   │
│  │  sky130_a  ████████████████ 588                        │   │
│  │  sky130_b  ██████████████████ 629                      │   │
│  │  ng45_a    ████████ 267                                │   │
│  │  ...                                                   │   │
│  └────────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌─ Frequency 柱状图 ─────────────────────────────────────┐   │
│  │  ...                                                   │   │
│  └────────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌─ Power 柱状图 ─────────────────────────────────────────┐   │
│  │  ...                                                   │   │
│  └────────────────────────────────────────────────────────┘   │
│                                                              │
│  [选择图表类型: 柱状图 ▾] [选择指标: 全部 ▾]                    │
└──────────────────────────────────────────────────────────────┘
```

**区域 C：空间 Map 横向对比（网格布局）**

```
┌──────────────────────────────────────────────────────────────┐
│  空间 Map 横向对比                          [选择 Map 类型 ▾]  │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  Map 类型: [Std-cell密度] [Pin密度] [EGR溢出] [布线需求] [DRC密度] │
│                                                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │ sky130_a │ │ sky130_b │ │ sky130_t │ │  ng45_a  │        │
│  │ [热力图] │ │ [热力图] │ │ [热力图] │ │ [热力图] │        │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘        │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │  ng45_b  │ │  ng45_t  │ │ asap7_a  │ │ asap7_b  │        │
│  │ [热力图] │ │ [热力图] │ │ [热力图] │ │ [热力图] │        │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘        │
│                                                              │
│  关键要求: 统一色标 — 所有图用同一个 color scale，                │
│  红色=高密度/高违例，蓝色=低密度/低违例。全零图明确标注。         │
└──────────────────────────────────────────────────────────────┘
```

**区域 D：单设计详情（可展开）**

```
┌──────────────────────────────────────────────────────────────┐
│  aes_sky130_a 详情                          [展开/折叠]       │
├──────────────────────────────────────────────────────────────┤
│  ┌─ 阶段纵向表 ───────────────────────────────────────────┐  │
│  │ Stage     │ Runtime │ Memory │ Instances │ WNS │ ...   │  │
│  │ Floorplan │ 0.0s    │ 0.0MB  │ 15,528    │ N/A │ ...   │  │
│  │ Placement │ 17.3s   │ 1016MB │ 15,528    │ N/A │ ...   │  │
│  │ CTS       │ 3.7s    │ 202MB  │ 15,549    │ N/A │ ...   │  │
│  │ Routing   │ 1438s   │ 0.0MB  │ 15,549    │ -6.5│ ...   │  │
│  │ Filler    │ 1.8s    │ 93.7MB │ 15,549    │ N/A │ ...   │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌─ 阶段版图  ─────────────────────────────────────────────┐  │
│  │ [Floorplan图] [Placement图] [CTS图] [Routing图] [GDS图]  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌─ 空间 Map (16 张)  ─────────────────────────────────────┐  │
│  │ [密度图×9] [EGR×3] [布线×3] [DRC×1]                    │  │
│  │ 每张图标注: non-zero 比例、最大值、原始 CSV 链接         │  │
│  └──────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

**区域 E：同工艺纵向结论（按 PDK 分组的变量扫描分析）**

> 对应参考文档 aes11_detailed_comparison.md 第 6 节。横向对比回答"哪个配置好"，
> 纵向结论回答"变量往哪个方向调有效"——这是对比实验的教育核心。

```
┌──────────────────────────────────────────────────────────────┐
│  同工艺纵向结论                          [自动生成 · 规则模板]  │
├──────────────────────────────────────────────────────────────┤
│  ▸ sky130 (4/4 完成 GDS)                                     │
│    最优 DRC: aes_sky130_t (143,411) · 最快布线: aes_sky130_t  │
│    利用率 35%→25%: EGR 线长 +6.4%, DRC 下降 6.5%             │
│    (153,418 → 143,411)                                       │
│    📌 密度缓解有效, 但改善幅度不足以接近 clean                │
│                                                              │
│  ▸ nangate45 (3/3 完成 GDS)                                  │
│    最优 DRC: aes_nangate45_a (18,297) · 最快布线: ..._a      │
│    利用率 35%→25%: EGR 线长 +7.0%, DRC 上升 17.4%            │
│    (18,297 → 21,485)                                         │
│    📌 更宽松的面积没有改善 DRC — 主导因素不是密度,            │
│       而是规则映射/轨道-via 映射/路由代价                     │
│                                                              │
│  ⚠️ 重复性检查: aes 与 aes_sky130_b 目标利用率同为 30%,       │
│     结果几乎一致 → baseline 重复性通过 (两者不能作为独立样本) │
└──────────────────────────────────────────────────────────────┘
```

生成规则（**规则模板，不依赖 LLM**）：

| 步骤 | 规则 |
|------|------|
| 分组 | 结果按 PDK 分组，每组单独生成一条结论 |
| 完成率 | `N/M 完成 GDS`（M=该 PDK 的组合数） |
| 最优/最快 | 组内 DRC 最小值、布线耗时最小值的配置名 |
| 变量扫描 | 识别数值型变量（如利用率 35%→25%），计算首末配置的指标变化率：`EGR WL Δ%`、`DRC Δ%`（含绝对值） |
| 解释判断 | DRC 随放宽下降 → "密度缓解有效"；DRC 不降反升 → "主导因素不是密度，而是规则/轨道-via 映射/路由代价" |
| 重复性检查 | 两组配置变量值完全相同且结果偏差 < 5% → 标注 baseline 重复性通过，并提示"不能作为独立样本扩大统计显著性" |
| 多变量 | 若实验含多个变量，纵向结论按"固定其他变量、扫描单变量"的方式逐变量生成 |

#### 5.2.4 后端：对比实验执行器

```
对比实验执行流程：

1. 前端提交实验配置 (JSON) → API
   {
     "design": "aes",
     "variables": {
       "pdk": ["sky130", "nangate45", "asap7", "ics55"],
       "utilization": [0.35, 0.30, 0.25]
     },
     "base_config": {"frequency": 100, "corners": ["TYP"]}
   }

2. API 展开为任务列表 (笛卡尔积)
   → 24 个独立任务，每个任务 = 一组确定的配置参数

3. 任务调度执行
   - 并行度由服务器资源决定（如最多同时跑 4 个）
   - 每个任务独立跑完整 Flow (Yosys 综合 → iEDA 物理设计)
   - WebSocket 实时推送每个任务的进度

4. 结果汇总
   - 每个任务完成后，提取关键指标 (WNS/Fmax/Power/Area/DRC/...)
   - 存入对比实验数据库
   - **生成同工艺纵向结论** (区域 E, 规则模板): 按 PDK 分组 → 完成率/最优配置/
     变量扫描变化率 → 解释判断 (密度有效 or 主导因素非密度) + 重复性检查

5. 生成对比报告
   - 横向总表 (CSV/JSON)
   - 横向柱状图 (Recharts 数据)
   - 空间 Map (统一色标的 PNG 网格)
   - 单设计详情 (可展开)
```

#### 5.2.5 对比实验数据模型

```
Experiment
├── experiment_id
├── name: "AES 多PDK多利用率对比"
├── design: "aes"
├── variables: {pdk: [...], utilization: [...]}
├── created_at
├── status: "running" | "completed" | "failed"
│
└── ExperimentRun (每个组合一个)
    ├── run_id
    ├── config: {pdk: "sky130", utilization: 0.35}
    ├── status: "queued" | "running" | "completed" | "failed"
    │
    └── RunMetrics
        ├── die_area_um2
        ├── cell_count
        ├── setup_wns_ns
        ├── hold_wns_ns
        ├── fmax_mhz
        ├── power_mw
        ├── eg_routed_wl_um
        ├── drc_violations
        ├── routing_runtime_s
        ├── ir_drop (可为 null)
        │
        └── StageMetrics[] (每阶段)
            ├── stage_name
            ├── runtime_s
            ├── memory_mb
            ├── instances
            └── ...
```

#### 5.2.6 空间 Map 热力图渲染方案

iEDA 产出的密度/拥塞/DRC 数据是 CSV 网格文件（如 `place_stdcell_density.csv`）。对比实验需要：

1. **统一色标**：所有同类型 Map 使用完全相同的 color scale（如 stdcell_density 统一 0-1.0 范围，DRC 统一对数色标）
2. **批量渲染**：服务端 Python 脚本（matplotlib/seaborn）批量读取 CSV → 渲染 PNG
3. **网格布局**：前端将多个设计的同类型 Map 排列成网格，便于肉眼比较
4. **全零图标注**：如果某 Map 全为零，显示空白 + "全零"标注，而非拉伸成伪热点

---

## 六、生成 Flow 的方案

### 6.0 设计哲学：Agent 时代的芯片设计流程

**传统流程的核心局限**（为什么需要从固定流程改造为 Agent 决策系统）：

传统芯片设计采用端到端粗放式全流程跑通逻辑——RTL 来了就按固定顺序从头跑到尾，跑完看结果，有问题再从头修。这种模式：
- 仅保障流程正常运行，**无精细化环节分析**——不知道问题出在哪个环节、根因是什么
- **修复效率低且针对性差**——一个 DRC 违例可能根因在 placement，但传统流程只能从 routing 反复试
- **修复范围是全量遍历**——修一个问题可能导致全流程重跑，而不是只重跑受影响的步骤

**Agent 时代新流程的核心特征**（本方案的设计哲学）：

类比医院："先检查，再诊疗"，而非"直接开刀，切开看哪里不对"。

```
传统:  RTL → [全流程跑通] → 看结果 → 有问题 → [全流程重跑] → ...
           ↑                                      ↑
          无中间分析步骤                    修复范围 = 全量

新:    RTL → [前置多维度分析] → 精准定位 → [针对性修复] → [增量重跑]
           ↑                      ↑            ↑              ↑
      分析约束/特性/         只有少数步骤     只修需要修      只重跑受
      路径分布等            需要修复         的环节         影响的步骤
```

**精细化分析不会提升整体成本**——恰恰相反：
- 花 6 秒跑 synth+iSTA（摸清 WNS）→ 避免花 20 分钟跑一个注定失败的 physical flow
- 花 30 秒诊断 WNS 根因（I/O约束/clock_uncertainty/WLM/lib/effort）→ 避免盲目降频
- 花 1 轮试修 DRC 观察 Hold 是否联动 → 避免修了 5 轮才发现需要联合回溯

**这就是 5 Sheet 决策系统存在的根本原因**：

| 传统固定流程 | 5 Sheet 改造 | 哲学对应 |
|------------|-------------|---------|
| 7 阶段步骤按固定顺序执行 | Sheet 1: Capability_Pool（能力打散为原子项） | Agent 根据分析结果**选择性拼装**，而非全量遍历 |
| 无决策逻辑，"跑完看结果" | Sheet 2: Solution_Lifecycle + Sheet 3: Agent_Decision | **先分析场景+设计特征**，再决定跑什么 |
| 无异常处理，"挂了重来" | Sheet 4: Exception_Handling（诊断子表 + 卡住信号 + 决策树） | **先诊断根因**，再针对性修复 |
| 无收敛概念，"跑到干净为止" | Sheet 5: Scenario_Examples（活动 2 每轮增量重跑） | **缩小修复范围**，只重跑受影响步骤 |

### 6.1 改造后的 Sheet 结构

```
原文件:
  Full_Flow (单 Sheet, 7 阶段固定流程, 8 列)

改造后 (5 个 Sheet):
  Sheet 1: Capability_Pool        ← 原子能力池（原 7 阶段步骤打散重组）
  Sheet 2: Solution_Lifecycle     ← Solution 生命周期：3 种活动模型（新增，顶层调度器）
  Sheet 3: Agent_Decision         ← Flow 拼装规则（新增，服务于 Flow 执行和归档）
  Sheet 4: Exception_Handling     ← 异常处理 + 收敛策略（新增，服务于 Flow 执行中的闸门检查和收敛循环）
  Sheet 5: Scenario_Examples      ← 完整 Solution 实例（新增，展示 5 张 Sheet 如何协同）
```

**5 张 Sheet 之间的关系（关键，避免割裂感）**：

```
┌──────────────────────────────────────────────────────────────────┐
│                  Sheet 2: Solution_Lifecycle                      │
│                  顶层调度器：3 种活动                               │
│                                                                  │
│  活动 1: 执行 Flow ──→ 活动 2: 收敛循环 ──→ 活动 3: 归档交付       │
│       │                      │                    │              │
│       ▼                      ▼                    ▼              │
│  每步用基础闸门检查      用 Sheet 4            用 Sheet 3         │
│  做步进闸门检查         做诊断+决策           拼装签核 Flow        │
│       │                      │                                   │
│  跑完后用 Sheet 4         回溯后用 Sheet 3                        │
│  做整体诊断               + Sheet 1 重拼装                        │
└──────────────────────────────────────────────────────────────────┘

Sheet 5 (Scenario_Examples) = 上述协同过程的具体演示
```

> 5 张 Sheet 的详细关系（角色分工表、两层闸门区别、协同流程图）见 [9.3](#93-5-张-sheet-之间的关系)。

### 6.2 Sheet 1: Capability_Pool（能力池）

**目的**：把原 7 阶段的所有步骤打散为独立的原子能力项。每个能力项是一个最小完整单元，有明确的输入、输出和通过标准。

**列定义**（共 12 列）：

| 列名 | 说明 | 示例 |
|------|------|------|
| `capability_id` | 唯一标识 | `RTL-001` |
| `capability_name` | 能力名称 | Verilator 语法编译 |
| `description` | 做什么 | 将 RTL 编译为 C++ 模型，检查语法和可综合性 |
| `stage_group` | 所属阶段分组 | 阶段1: RTL 设计与生成 |
| `preconditions` | 前置条件（必须满足才能执行） | RTL 文件存在且 filelist 完整 |
| `input_artifacts` | 输入文件/数据 | .v/.sv 文件 + filelist |
| `output_artifacts` | 输出文件/数据 | 编译日志 + 语法错误列表 |
| `applicable_when` | 适用条件（什么情况下需要做） | 所有设计（必做） |
| `skip_when` | 跳过条件（什么情况下不需要做） | 无（基础检查不可跳过） |
| `primary_tool` | 主要工具 | Verible / Verilator |
| `typical_duration` | 典型耗时 | 5-30 秒（取决于设计规模） |
| `pass_criteria` | 通过标准（铜/银/金三级） | 🥉 语法 Error=0; 🥈 Warning≤5; 🥇 Lint 通过 + Style Check 通过 |

**覆盖范围**：从 RTL 生成到 GDS 交付的全部能力项（共 8 个阶段，~55 项原子能力）。

以下按阶段列出完整能力池。每个能力项包含 Agent 做决策所需的全部字段。

---

#### 阶段 0：RTL 设计与生成

| ID | 能力名称 | 前置条件 | 适用/跳过条件 | 主要工具 | 典型耗时 | 通过标准 🥉/🥈/🥇 |
|----|---------|---------|-------------|---------|---------|-------------------|
| RTL-001 | NL→RTL 生成 | 用户输入自然语言需求 | 适用: AI生成模式; 跳过: 手写/上传模式 | ChipMATE + LLM 后端 | 10-60s (取决于设计复杂度) | 🥉 Verilator 编译通过; 🥈 Icarus 仿真波形匹配预期; 🥇 交叉验证匹配率=100% + Verible Lint 通过 + Style Check 通过 |
| RTL-002 | RTL 代码手写 | Monaco 编辑器加载完成 | 适用: 手写模式; 跳过: AI生成/上传模式 | Monaco Editor | — | 🥉 语法无错误; 🥈 无 Latch 推断; 🥇 Verible Lint 通过 + Style Check 通过 |
| RTL-003 | RTL 文件上传 | 用户有本地 .v/.sv 文件 | 适用: 上传模式; 跳过: AI生成/手写模式 | 文件上传组件 | — | 🥉 文件格式正确; 🥈 自动识别顶层 module; 🥇 filelist 自动生成 |
| RTL-004 | RTL 语法检查 | RTL 文件存在 | 适用: 所有模式; 跳过: 无 | Verible | 5-30s | 🥉 0 Error; 🥈 Warning≤5; 🥇 Lint 通过 + Style Check 通过 |
| RTL-005 | RTL 编译 | RTL 语法检查通过 | 适用: 所有模式; 跳过: 无 | Verilator (`--lint-only`) | 5-30s | 🥉 编译无 Fatal; 🥈 0 Warning; 🥇 **综合检查通过**（注：Verilator 是仿真编译器，`--lint-only` 只检查语法和简单语义，不检查可综合性。金牌需额外跑 Yosys `synth` 确认无可综合性问题） |
| RTL-006 | 基础功能仿真 | RTL 编译通过 + testbench 存在 | 适用: 所有模式; 跳过: 无 testbench 时自动生成简单激励 | Icarus (默认) / Verilator (大设计) | 10s-2min | 🥉 仿真完成无 crash; 🥈 关键信号波形正确; 🥇 仿真 log 无 X 态传播 |
| RTL-007 | 波形查看 | 仿真完成且有 VCD/FST 输出 | 适用: 需要波形调试时; 跳过: 只看报告 | Surfer (大) / vcdrom (小) | 即时 (前端渲染) | 🥉 波形可加载; 🥈 信号分组+光标测量可用; 🥇 源码-波形双向联动 |

---

#### 阶段 1：RTL 交付接收与准入检查

| ID | 能力名称 | 前置条件 | 适用/跳过条件 | 主要工具 | 典型耗时 | 通过标准 🥉/🥈/🥇 |
|----|---------|---------|-------------|---------|---------|-------------------|
| CHK-001 | 交付清单检查 | 用户提交了设计包 | 适用: 上传/手写模式; 跳过: AI 生成模式 (自动生成清单) | 内部脚本 | 1-5s | 🥉 清单文件存在; 🥈 清单内容与文件数匹配; 🥇 自动补全缺失项 |
| CHK-002 | RTL 导入与解析 | RTL 文件路径明确 | 适用: 所有模式; 跳过: 无 | Verible / Yosys | 5-30s | 🥉 顶层 module 找到; 🥈 所有文件可解析; 🥇 层级关系自动生成 |
| CHK-003 | filelist/宏/include 检查 | RTL 导入成功 | 适用: 所有模式; 跳过: 无 | 内部脚本 + Verible | 5-15s | 🥉 无缺失 `include; 🥈 宏定义无冲突; 🥇 自动补全缺失路径 |
| CHK-004 | 设计层级展开 (elaboration) | filelist 检查通过 | 适用: 所有模式; 跳过: 无 | Yosys / Verilator | 10-60s | 🥉 无 Fatal error; 🥈 层级完整; 🥇 自动输出层级报告 |
| CHK-005 | IP/存储器/黑盒检查 | elaboration 完成 | 适用: 设计包含 IP/Memory 实例; 跳过: 纯标准单元设计 | 内部脚本 | 5-15s | 🥉 黑盒声明存在; 🥈 IP 模型版本匹配; 🥇 自动标记缺失模型 |
| CHK-006 | SDC/UPF 基础检查 | SDC/UPF 文件存在 | 适用: 有时序约束或低功耗意图; 跳过: 纯学习无约束场景 | iSTA / 内部脚本 | 5-20s | 🥉 文件可解析; 🥈 时钟定义完整; 🥇 IO 约束 + 时钟组检查通过 |
| CHK-007 | Waiver 基线导入 | 有历史运行记录 | 适用: 非首次运行; 跳过: 首次运行 | 内部脚本 | 1-3s | 🥉 历史 waiver 已导入; 🥈 与当前版本对齐; 🥇 自动标注已修复项 |

---

#### 阶段 2：RTL 静态质量与前端签核

| ID | 能力名称 | 前置条件 | 适用/跳过条件 | 主要工具 | 典型耗时 | 通过标准 🥉/🥈/🥇 |
|----|---------|---------|-------------|---------|---------|-------------------|
| QA-001 | Lint 代码规范检查 | RTL 通过准入检查 | 适用: 所有设计; 跳过: 无 | Verible | 10-60s | 🥉 0 Fatal; 🥈 Warning 分类评审完成; 🥇 所有 Waiver 有 Owner |
| QA-002 | CDC 跨时钟域检查 | 设计含 ≥ 2 个时钟域 | 适用: 多时钟设计; 跳过: 单时钟设计 (Agent 自动检测) | **开源无成熟 CDC 工具** | — | 🥉 手工标注所有跨域路径; 🥈 每条路径有对应的同步器或约束; 🥇 跨域路径用 SymbiYosys 逐条证明（需手写 SVA property，非自动检测） |
| QA-003 | RDC 跨复位域检查 | 设计含 ≥ 2 个复位域 | 适用: 多复位设计; 跳过: 单复位设计 | **同 CDC，开源无成熟 RDC 工具** | — | 🥉 手工标注所有复位域; 🥈 每条跨域复位路径有同步方案; 🥇 复位树恢复时序满足设计要求 |
| QA-004 | 低功耗 UPF 基础检查 | UPF/CPF 文件存在 | 适用: 有低功耗意图; 跳过: 无 UPF 文件 (Agent 自动跳过) | 内部脚本 (UPF 语法解析) + iSTA (多电压域时序) | 10-30s | 🥉 UPF 文件语法可解析; 🥈 电压域/Level Shifter/Isolation 定义完整; 🥇 多电压域时序在 iSTA 中无违例（注：**开源无完整 UPF 一致性验证工具**。iSTA 仅能从时序角度检查多电压域，不能替代商业工具如 VC LP / Conformal LP 的 UPF 物理实现验证） |
| QA-005 | SDC 约束质量检查 | SDC 文件存在 | 适用: 有时序约束; 跳过: 纯功能验证 | iSTA + 内部脚本 | 10-60s | 🥉 时钟定义完整; 🥈 false/multicycle 路径合理; 🥇 IO 约束覆盖全部端口 |
| QA-006 | Formal Property Smoke | .sva/.psl 属性文件存在 | 适用: 有属性文件; 跳过: 无属性文件 | SymbiYosys (BMC) | 10s-2min | 🥉 属性可编译; 🥈 BMC 深度≥10 无违例; 🥇 关键协议属性全部通过 |
| QA-007 | Smoke 仿真 | testbench 存在 | 适用: 有 testbench; 跳过: 无 testbench (Agent 提示建议补充) | Icarus | 30s-5min | 🥉 仿真完成; 🥈 关键测试用例通过; 🥇 无 X 态 + 断言全部通过 |
| QA-008 | 问题/Waiver 评审 | 以上检查产生了问题清单 | 适用: 有问题需要记录; 跳过: 全部通过 | Agent 辅助分类 | 人工/Agent 协作 | 🥉 问题已分类; 🥈 Waiver 有审批; 🥇 所有问题有 Owner + Deadline |

---

#### 阶段 3：逻辑综合与 DFT

| ID | 能力名称 | 前置条件 | 适用/跳过条件 | 主要工具 | 典型耗时 | 通过标准 🥉/🥈/🥇 |
|----|---------|---------|-------------|---------|---------|-------------------|
| SYN-001 | 综合前 SDC 约束检查 | SDC 文件存在 | 适用: 需要综合; 跳过: 仅仿真 | iSTA | 5-20s | 🥉 约束可读; 🥈 无不合理约束; 🥇 时钟关系+IO 约束完整 |
| SYN-002 | 逻辑综合 | RTL + LIB + SDC 齐全 | 适用: 需要门级网表; 跳过: 仅仿真 | Yosys | 10s-5min | 🥉 综合完成; 🥈 无 blackbox; 🥇 面积/逻辑级数在预期内 |
| SYN-003 | 综合后 LEC | 综合网表 + 原始 RTL 均存在 | 适用: 需要确认功能等价; 跳过: 纯学习场景 | Yosys `equiv` / SymbiYosys | 10s-3min | 🥉 LEC 完成; 🥈 100% Pass; 🥇 所有关键点等价 |
| SYN-004 | 综合后 STA | 综合网表 + SDC + LIB | 适用: 需要进入物理设计前评估; 跳过: 仅仿真 | iSTA | 5-30s | 🥉 STA 完成; 🥈 WNS ≥ 0ns; 🥇 Slack ≥ 时钟周期的 20%（例如 100MHz → T=10ns → WNS ≥ +2ns） |
| SYN-005 | DFT 扫描链插入 | 综合网表 + DFT 配置 | 适用: 需要可测性; 跳过: 教学/竞赛不做流片 | Yosys `dft` 命令 | 10s-2min | 🥉 扫描链插入完成; 🥈 DFT DRC 通过（Yosys 仅基础检查）；🥇 扫描链连通性验证通过（注：**开源无成熟 DFT 工具**。Yosys `dft` 仅基础 scan insertion，无 MBIST/LBIST/BSCAN。流片需商业工具如 Tessent/TestMAX/Modus） |
| SYN-006 | DFT 后 LEC | DFT 网表 + 原始 RTL | 适用: DFT 已插入; 跳过: 无 DFT | Yosys `equiv` | 10s-1min | 🥉 LEC 完成; 🥈 100% Pass; 🥇 无功能损失 |
| SYN-007 | ATPG 测试向量生成 | DFT 网表就绪 | 适用: 需要测试向量; 跳过: 无 DFT | **开源无成熟 ATPG 工具** | — | 🥉 不适用; 🥈 不适用; 🥇 不适用——标注为"需商业工具"（注：Yosys 无 ATPG 能力。流片需 Tessent/TestMAX/Modus 等商业 ATPG 工具） |
| SYN-008 | 综合后 PPA 评估 | 综合网表 + STA + 功耗结果 | 适用: 需要 PPA 数据; 跳过: 纯学习 | iSTA + Agent 汇总 | 5-10s | 🥉 面积/频率/功耗数据完整; 🥈 在预算内; 🥇 有优化空间的分析报告 |

---

#### 阶段 4：物理规划

| ID | 能力名称 | 前置条件 | 适用/跳过条件 | 主要工具 | 典型耗时 | 通过标准 🥉/🥈/🥇 |
|----|---------|---------|-------------|---------|---------|-------------------|
| PHY-001 | Floorplan | 综合网表 + LEF/DEF + 约束 | 适用: 需要物理实现; 跳过: 仅综合评估 | iEDA iFP | 5-30s | 🥉 完成; 🥈 Utilization 合理(不>80%); 🥇 Macro 通道+Pin 方向无风险 |
| PHY-002 | Fix Fanout | Floorplan 完成 | 适用: 高扇出 net 存在; 跳过: 无高扇出 | iEDA iNO | 2-10s | 🥉 完成; 🥈 无 max_fanout 违例; 🥇 高扇出网络全部插入 buffer tree |
| PHY-003 | Placement | Floorplan + FixFanout 完成 | 适用: 需要物理实现; 跳过: 仅 floorplan 评估 | iEDA iPL | 10-60s | 🥉 完成; 🥈 无重叠; 🥇 HPWL + 时序预估在目标内 |
| PHY-004 | CTS | Placement 完成 + SDC | 适用: 有时钟 (时序逻辑); 跳过: 纯组合逻辑 | iEDA iCTS | 5-30s | 🥉 完成; 🥈 Skew 在目标内; 🥇 时钟树深度+功耗合理 |
| PHY-005 | 驱动优化 (optDrv) | CTS 完成 | 适用: 有时序违例; 跳过: setup/hold 已 clean | iEDA iTO | 5-30s | 🥉 完成; 🥈 max_transition/cap 违例清零; 🥇 无新增 setup 违例 |
| PHY-006 | Hold 优化 (optHold) | optDrv 完成 | 适用: hold 违例存在; 跳过: hold 已 clean | iEDA iTO | 5-30s | 🥉 完成; 🥈 Hold WNS ≥ 0; 🥇 无新增 setup 违例 |
| PHY-007 | Legalization | Placement/CTS 完成 | 适用: 需合法化; 跳过: 已合法 | iEDA iPL | 3-10s | 🥉 完成; 🥈 0 非法放置; 🥇 合法化后时序退化 < 5% |
| PHY-008 | 全局布线 | Legalization 完成 | 适用: 需要布线; 跳过: 仅到 placement | iEDA iRT | 10s-2min | 🥉 完成; 🥈 Congestion < 100%; 🥇 无 EGR overflow 红区 |
| PHY-009 | 详细布线 | 全局布线完成 | 适用: 需要最终布线; 跳过: 仅评估 | iEDA iRT | 1-20min | 🥉 完成; 🥈 Short/Open=0; 🥇 Post-route STA WNS≥0 |
| PHY-010 | Filler/Decap 插入 | 详细布线完成 | 适用: 需要完整版图; 跳过: 仅到布线 | iEDA iPL | 3-10s | 🥉 完成; 🥈 N-well 连续性检查通过; 🥇 密度检查初版通过 |

---

#### 阶段 5：实现收敛监控

| ID | 能力名称 | 前置条件 | 适用/跳过条件 | 主要工具 | 典型耗时 | 通过标准 🥉/🥈/🥇 |
|----|---------|---------|-------------|---------|---------|-------------------|
| IMP-001 | Post-Route STA | 详细布线完成 + SPEF 已抽取 | 适用: 需要时序签核; 跳过: 仅到布线 | iSTA | 10s-2min | 🥉 完成; 🥈 Setup WNS≥0; 🥇 Hold WNS≥0 + DRV 清零 |
| IMP-002 | Post-Route DRC (pre-fill) | 详细布线完成 | 适用: 需要检查物理规则; 跳过: 仅评估 | iEDA iDRC | 10s-5min | 🥉 完成; 🥈 违例分类明确; 🥇 0 blocking 违例 |
| IMP-003 | Early IR-Drop | PDN + 布线完成 + 活动文件 | 适用: 需要电源完整性; 跳过: 设计<5000门或仅学习 | iEDA iIR | 30s-5min | 🥉 完成; 🥈 静态 IR<5% VDD; 🥇 动态 IR<10% VDD |
| IMP-004 | 时序 ECO | Post-route STA 有违例 | 适用: 有 setup/hold 违例; 跳过: 已 clean | Agent 决策 + iEDA | 1-5min/轮 | 🥉 违例数减少; 🥈 主要违例清零; 🥇 全部清零 |
| IMP-005 | 收敛状态诊断 | 当前轮次的全部指标 | 适用: 每一轮收敛循环后; 跳过: 无 | Agent 分析 | 1-5s | 🥉 诊断报告生成; 🥈 问题分类+定位; 🥇 改进建议+回退方案 |

---

#### 阶段 6：STA 与可靠性签核

| ID | 能力名称 | 前置条件 | 适用/跳过条件 | 主要工具 | 典型耗时 | 通过标准 🥉/🥈/🥇 |
|----|---------|---------|-------------|---------|---------|-------------------|
| SIG-001 | 寄生抽取 (RC Extraction) | 布线数据库/DEF 就绪 | 适用: 需要精确时序; 跳过: 仅初步评估 | iEDA 内置 | 10s-3min | 🥉 完成; 🥈 SPEF 文件生成; 🥇 与 LEF/工艺 cross-check 通过 |
| SIG-002 | MCMM STA | SPEF + 多 corner LIB + SDC | 适用: 需要签核级时序; 跳过: 仅单 corner | iSTA | 30s-5min/corner | 🥉 所有 corner STA 完成; 🥈 所有 corner WNS≥0; 🥇 SI/OCV 分析通过 |
| SIG-003 | SI/串扰分析 | SPEF 含耦合电容 + STA 就绪 | 适用: 需要签核级; 跳过: 低频设计或教学 | iSTA (含 SI 分析) | 1-5min | 🥉 完成; 🥈 无关键串扰违例; 🥇 所有串扰路径都在 margin 内 |
| SIG-004 | 门级仿真 (GLS) | 门级网表 + SDF + testbench | 适用: 关键模块验证; 跳过: 教学/竞赛 (默认跳过) | Icarus/Verilator | 1-20min | 🥉 完成; 🥈 功能与 RTL 仿真一致; 🥇 时序反标后无 X 态传播 |
| SIG-005 | 功耗签核 | 布线后 SPEF + VCD/SAIF | 适用: 需要功耗数据; 跳过: 教学/竞赛 (可选) | iSTA (功耗分析) | 30s-5min | 🥉 完成; 🥈 动态+静态功耗在预算内; 🥇 IR-drop 与功耗数据一致 |
| SIG-006 | EM/IR 签核 | PDN 模型 + 电流数据 | 适用: 需要签核; 跳过: <5000门或教学 | iEDA iIR | 1-10min | 🥉 完成; 🥈 静态+动态 IR 达标; 🥇 EM 无违例 |
| SIG-007 | 签核级 ECO | 签核检查有违例 | 适用: 有违例需要修; 跳过: 全部 clean | Agent 决策 + iEDA | 1-10min/轮 | 🥉 违例减少; 🥈 最后一轮 ECO 无新违例; 🥇 全部签核指标通过 |

---

#### 阶段 7：物理验证与流片归档

| ID | 能力名称 | 前置条件 | 适用/跳过条件 | 主要工具 | 典型耗时 | 通过标准 🥉/🥈/🥇 |
|----|---------|---------|-------------|---------|---------|-------------------|
| PV-001 | Metal Fill | 详细布线完成 | 适用: 需要完整版图; 跳过: 仅评估 | iEDA 内置 | 10-60s | 🥉 完成; 🥈 密度在 Foundry 范围内; 🥇 无密度相关 DRC |
| PV-002 | Fill 后 STA 重跑 | Metal Fill + 重新抽取 SPEF | 适用: 需要签核; 跳过: 仅评估 | iSTA | 30s-3min | 🥉 完成; 🥈 Fill 后 WNS 未显著退化; 🥇 所有 corner WNS≥0 |
| PV-003 | DRC Final | Fill 后 GDS/DEF | 适用: 需要签核; 跳过: 教学/竞赛 | iEDA iDRC | 1-20min | 🥉 完成; 🥈 0 未豁免 Error; 🥇 全规则集通过 |
| PV-004 | LVS Final | Fill 后 GDS + 源网表 | 适用: 需要签核; 跳过: 教学/竞赛 | Netgen | 1-10min | 🥉 完成; 🥈 LVS Clean; 🥇 器件数+节点数完全匹配 |
| PV-005 | ERC/ANT/DFM | GDS 就绪 + Rule Deck | 适用: 需要签核; 跳过: 教学/竞赛 | iEDA 内置 | 1-10min | 🥉 完成; 🥈 0 未豁免 Error; 🥇 全规则集通过 |
| PV-006 | ECO 后 LEC | ECO 网表 + 原始 RTL | 适用: 有 ECO; 跳过: 无 ECO | Yosys `equiv` | 10s-2min | 🥉 完成; 🥈 100% Pass; 🥇 所有 ECO 点等价 |
| PV-007 | GDS/OASIS 导出 | 全部 PV 检查通过或已豁免 | 适用: 需要版图输出; 跳过: 仅评估 | iEDA 内置 | 10-60s | 🥉 GDS 文件生成; 🥈 可被 KLayout 正常读取; 🥇 文件大小合理+版本号已标记 |
| PV-008 | 归档发布 | GDS + 全部报告+输入数据 | 适用: 需要交付; 跳过: 教学/竞赛 | 内部脚本 | 5-30s | 🥉 归档包完整; 🥈 Manifest + 脚本 Hash 可追溯; 🥇 可复现性验证通过 |

### 6.3 Sheet 2: Solution_Lifecycle（Solution 生命周期）

**定位**：顶层调度器。定义 Solution 由 3 种活动组成，以及活动之间的流转关系。

#### 6.3.1 活动按场景决定

并非所有场景都需要全部 3 种活动。**体验和课程场景不进入物理实现**，6.3-6.6 描述的物理 Flow 机制根本不会被触发。

| 场景 | 活动 1<br>执行 Flow | 活动 2<br>收敛循环 | 活动 3<br>归档交付 |
|------|:---:|:---:|:---:|
| **高中生体验** | 阶段 1: AI生成+编译+仿真 → 结束 | — | — |
| **本科课程** | 阶段 1+2: +验证+覆盖率 → 结束 | — | — |
| **竞赛 PPA 对比** | ✅ 阶段 3 物理 Flow (综合→STA) | ✅ 有止损 | ✅ 对比报告 |
| **科研评估** | ✅ 阶段 3 物理 Flow (综合→GDS) | ✅ 可选 | 视研究目标 |
| **流片签核** | ✅ 阶段 3 物理 Flow (综合→GDS) | ✅ 必须 clean | ✅ 签核交付 |

#### 6.3.2 3 种活动定义

| 属性 | 活动 1: 执行 Flow | 活动 2: 收敛循环 | 活动 3: 归档交付 |
|------|------------------|-----------------|-----------------|
| **目的** | Agent 拼装 Flow 并逐步执行，每步通过闸门检查才进入下一步 | 诊断问题→决定回溯→重拼装→重跑→再检查 | 生成最终交付物 |
| **次数** | 1 次 | N 次（竞赛有止损，流片迭代到 clean） | 视场景：竞赛 1 次报告 / 流片 1 次签核 / 科研视目标 |
| **调用的 Sheet** | Sheet 3（拼装 Flow）<br>Sheet 1 铜标准（每步闸门）<br>Sheet 4（跑完后整体诊断） | Sheet 4（诊断+决策）<br>Sheet 3 + Sheet 1（回溯后重拼装） | Sheet 3（签核 Flow） |



#### 6.3.3 活动之间的流转关系

```
用户在阶段 1/2 完成 RTL 生成和验证
        │
        │  (场景=体验/课程 → 结束, 不进入下面)
        │  (场景=竞赛/科研/流片 → 继续)
        ▼
┌──────────────────────────────────────────────────────────────┐
│                    活动 1: 执行 Flow                           │
│                                                              │
│  Step 1: 调用 Sheet 3 → 根据场景拼装 Flow                      │
│                                                              │
│  Step 2: 逐步执行, 每步后用 Sheet 1 铜标准检查产出物:            │
│                                                              │
│    synth ──→ 铜标准: 网表>1KB + 无 Fatal? ──→ ✅ 继续          │
│    iSTA  ──→ 铜标准: 报告生成? ──→ ✅ 继续                     │
│              │  ← 这就是旧版所谓的"摸底",                          │
│              │    实质是 Flow 的第一个闸门检查点                    │
│              │                                                │
│    floorplan ──→ 铜标准: DEF>1KB? ──→ ✅ 继续                   │
│    placement ──→ 铜标准: DEF>1KB + 无非法放置? ──→ ✅ 继续       │
│    ...                                                        │
│    routing ──→ 铜标准: 布线完成?                                │
│              ✅ 继续 → ... → GDS                                │
│              🚫 阻塞(产不出 GDS) → 活动 1 中断, 进入活动 2       │
│                                                              │
│  Step 3: 跑完后, 调用 Sheet 4 (6.5.1) 做整体诊断:              │
│    全部指标 clean?                                            │
│      Yes + 场景需要归档 → 进入活动 3                            │
│      Yes + 场景不需要归档 → Solution 完成                       │
│      No  → 进入活动 2                                          │
└────────────────────┬─────────────────────────────────────────┘
                     │ 有需要修复的问题
                     ▼
┌──────────────────────────────────────────────────────────────┐
│                 活动 2: 收敛循环 (N 次迭代)                      │
│                                                              │
│  每轮执行:                                                    │
│   ① 调用 Sheet 4 (6.5.1) → 诊断本轮指标, 判断卡住信号         │
│   ② 调用 Sheet 4 (6.5.2) → 决策:                              │
│       问题独立? → 逐个修复 (单步增量)                           │
│       问题关联? → 联合修复 (多步回溯)                           │
│       连续N轮无改善? → 止损 (给出降频/换策略建议, 结束)         │
│   ③ 确定回溯目标后:                                           │
│       调用 Sheet 3 → 从回溯点重新拼装 Flow                      │
│       调用 Sheet 1 → 查询回溯点之后的能力                       │
│   ④ 从回溯点重跑到 GDS, 回到 ①                                │
│                                                              │
│  终止条件: 全部 clean → 活动 3 / 止损 → 结束                   │
└────────────────────┬─────────────────────────────────────────┘
                     │ 全部指标 clean
                     ▼
┌──────────────────────────────────────────────────────────────┐
│               活动 3: 归档交付 (视场景)                         │
│                                                              │
│  竞赛: 生成 PPA 对比报告 (非 GDS signoff)                      │
│  流片: 多 corner STA + DRC/LVS final + GDS + 文档 + 版本锁定  │
│  科研: 视研究目标                                              │
└──────────────────────────────────────────────────────────────┘
        │
        ▼
   Solution 完成
```

**关于 synth→iSTA 后 WNS 异常的说明**：WNS 为负不一定是设计本身的问题。Agent 应先走诊断清单排除配置问题（见 6.5.1 综合阶段的诊断子表），清单排除后仍异常才建议降频或修改 RTL。

#### 6.3.4 Sheet 1 铜标准 vs Sheet 4 卡住信号：两级闸门

活动 1 中每一步都在做闸门检查，但用的是**两个不同级别的标准**：

| 闸门级别 | 来源 | 检查什么 | 不通过怎么处理 |
|---------|------|---------|--------------|
| **铜标准** | Sheet 1 每个能力项的 `pass_criteria` | 产出物是否可用？文件是否存在、大小是否合理、有无 Fatal Error | 🚫 阻塞——活动 1 中断，进入活动 2 |
| **卡住信号** | Sheet 4 (6.5.1) | 指标是否可接受？WNS 是否超标、DRC 是否可容忍、面积是否在预算内 | ⚠️ 记录到问题清单，活动 1 继续跑完，跑完后整体诊断决定进入活动 2 还是活动 3 |

**举例**：
- routing 没产出 DEF → 铜标准不通过 → **停止**，进活动 2 修 routing
- routing 产出了 DEF，但 DRC=142 → 铜标准通过（文件存在），但卡住信号触发了（违例偏多）→ **记录下来继续跑**，活动 1 跑完后诊断决定修不修

#### 6.3.5 活动 2 为什么需要调用 Sheet 3 + Sheet 1

活动 2 决定"退回到 placement"后，不能假设之前的 Flow 配置仍然适用——回溯后的起点变了，需要重新拼装：

| 活动 2 步骤 | 使用的 Sheet |
|------------|-------------|
| 诊断 | Sheet 4 (6.5.1) |
| 决策回溯目标 | Sheet 4 (6.5.2) |
| **从回溯点重拼装 Flow** | **Sheet 3 + Sheet 1** |
| 止损检查 | Sheet 4 (止损策略) |

### 6.4 Sheet 3: Agent_Decision（Flow 拼装规则）

**目的**：定义 Agent 如何从 Capability_Pool 中挑选能力、拼装 Flow。四个决策维度。

#### 决策维度 1：截断点（Truncation）

| 场景 | 截断点 | 包含的能力组 | 跳过 |
|------|--------|------------|------|
| 高中生体验 | 编译 + 简单仿真 | RTL-001, SIM-001 | 综合及以后全部 |
| 本科课程 | 仿真 + 覆盖率 | RTL-001, SIM-001, SIM-002(覆盖率) | 综合及以后 |
| 竞赛对比 | 综合 + STA | RTL-001, SYN-001, STA-001 | 物理设计及以后 |
| 科研评估 | 物理设计 + STA | 全部能力，不含 sign-off 级检查 | DRC final / LVS final |
| 流片签核 | 全流程到 GDS | 全部能力 | 无 |

#### 决策维度 2：跳过规则（Skip）

| 规则 ID | 条件 | 跳过的能力 |
|---------|------|-----------|
| SKIP-01 | 设计只有 1 个时钟域 | CDC 检查 (原阶段2 CDC/RDC) |
| SKIP-02 | 无 UPF/CPF 文件 | 低功耗检查 (原阶段2) |
| SKIP-03 | 无 formal property 文件 | formal property smoke |
| SKIP-04 | 纯组合逻辑 | CTS, optHold |
| SKIP-05 | 用户目标是"学 RTL" | 综合及以后全部 |
| SKIP-06 | 设计规模 < 5000 门 | IR-drop 分析（可跳过） |
| SKIP-07 | 无 MBIST/LBIST 配置 | MBIST/LBIST 插入 |

#### 决策维度 3：强度参数（Intensity）

同一项能力，在不同的置信度需求下，执行深度不同。Agent 不硬编码"竞赛=几个 corner"，而是根据**场景的具体评估目标**决定。

**STA Corner 选择逻辑**（示例——说明 Agent 的推理过程，而非固定映射）：

| 场景细分 | Corner 选择 | 推理依据 |
|---------|------------|---------|
| 体验/入门学习 | 1 (TYP) | 只需验证功能正确性，不需要 signoff |
| 课程作业 | 1 (TYP) | 证明设计能跑即可 |
| 竞赛 — PPA 优化型（比 Fmax / 面积 / 功耗） | 2 (TYP+SLOW) | SLOW corner 决定 Setup/Fmax——PPA 排名的核心依据 |
| 竞赛 — 完整签核型（模拟真实流片） | 3 (TYP+SLOW+FAST) | FAST corner 决定 Hold——需要证明所有 PVT 条件下都正确 |
| 竞赛 — 低功耗专题 | 2+ (TYP+SLOW，可能加低温 corner) | 功耗与温度强相关，可能需要额外 corner |
| 科研评估 — 架构对比 | 1 (TYP) | 关心相对排名而非绝对 signoff |
| 科研评估 — 流片前验证 | 3+ (全 corner) | 接近 signoff 级要求 |
| 流片签核 | 3+ (全 corner + SI/OCV) | 不可妥协 |

**其他强度参数**（同样遵循"Agent 根据目标推理"的逻辑）：

| 参数 | 推理依据 | 示例 |
|------|---------|------|
| 覆盖率目标 | 置信度越高，覆盖率要求越严 | 体验 60% → 课程 85% → 流片 95%+ |
| DRC 检查深度 | 越接近流片，规则集越全 | 基础规则 → 全规则 → +密度+天线+DFM |
| 布线迭代次数 | 需要多干净？ | 体验/竞赛 1 次 → 流片 迭代到 clean |
| 综合策略 | 优化什么？ | 默认 → 面积优先/时序优先 → 多策略竞优 |

#### 决策维度 4：工具替换（Substitution）

| 步骤 | 全开源 (默认) | 有商业 License |
|------|-------------|---------------|
| 仿真 | Icarus + Verilator | VCS / Xcelium |
| 综合 | Yosys | Design Compiler / Genus |
| STA | iSTA | PrimeTime / Tempus |
| 物理设计 | iEDA | ICC2 / Innovus |
| DRC/LVS | iDRC + Netgen | Calibre / IC Validator |

Agent 决策流程（描述 Agent 如何拼装 Flow，非可执行代码）：

```
function compose_flow(scene, design_features, available_tools):
    // Step 1: 确定截断点
    capabilities = Capability_Pool.filter(up_to: scene.truncation_point)
    
    // Step 2: 应用跳过规则
    for rule in SKIP_RULES:
        if rule.condition matches design_features:
            capabilities.remove(rule.skip_capabilities)
    
    // Step 3: 设置每项能力的强度参数
    for cap in capabilities:
        cap.intensity = INTENSITY_MAP[scene.confidence_level][cap.type]
    
    // Step 4: 替换工具（如果需要）
    if available_tools.has_commercial:
        capabilities = substitute_tools(capabilities, "commercial")
    
    return Flow(capabilities)
```

### 6.5 Sheet 4: Exception_Handling（异常处理 + 收敛策略）

**定位**：诊断工具。在**三个时间点**被调用：

| 时间点 | 用 6.5.1 做什么 | 用 6.5.2 做什么 |
|--------|---------------|---------------|
| **活动 1 每步执行后** | 对照 Sheet 1 铜标准——产出物可用吗？决定能否继续下一步 | — |
| **活动 1 跑完/中断后** | 对照 6.5.1 卡住信号——整体结果可接受吗？决定进入活动 2 还是活动 3 | — |
| **活动 2 每一轮后** | 对照 6.5.1 卡住信号——本轮有改善吗？触发止损了吗？ | 决策树判断：独立修/联合回溯/止损 |

#### 6.5.1 卡住信号与应对动作

定义每个阶段的常见"卡住信号"和 Agent 应对策略。

**列定义**：

| 列名 | 说明 |
|------|------|
| `stage` | 所在阶段 |
| `stuck_signal` | 卡住信号（可量化的阈值） |
| `severity` | WARNING（记录到问题清单，活动 1 继续） / BLOCKING（活动 1 中断，进入活动 2） |
| `action` | Agent 应对动作 |
| `backtrack_target` | 回退目标（如果需要跨阶段回退） |
| `max_retries` | 在当前阶段的最大重试次数 |
| `escalation` | 超过 max_retries 后的升级策略 |

**示例行**（含综合阶段 WNS 诊断子表）：

| stage | stuck_signal | severity | action | backtrack_target | max_retries | escalation |
|-------|-------------|----------|--------|-----------------|-------------|------------|
| 综合 | WNS < 0 (有违例) | WARNING | **先走诊断子表排除配置问题**（见下方）；排除后 WNS 仍 < 0 → 记录到问题清单 | — | — | 活动 1 跑完后整体判断 |
| 综合 | WNS < -2ns 且诊断子表已全部排除 | BLOCKING | 时序差距过大，建议降低目标频率或换更快的库 | — | — | 建议修改 RTL 架构/换工艺 corner |
| placement | HPWL 比预期大 50%+ | WARNING | 检查 floorplan 利用率；放宽 die 面积 | floorplan (调大面积) | 3 | 降频或接受面积膨胀 |
| routing | DRC 违例 > 1000 | BLOCKING | 分析违例类型（metal_short 多 → 增加布线迭代；pin_access → 退 placement） | routing (调参) 或 placement | 3 | 退回 placement，降低 density 5% |
| routing | DRC 连续 3 轮不降 | BLOCKING | 退回 placement，降低 density | placement | 2 | 退回 floorplan，增大 die 面积 10% |
| STA | SLOW corner WNS < 0 (单 corner 失败) | BLOCKING | 分析关键路径；检查是否 corner 约束过于悲观 | 综合 (换优化目标) 或 placement | 2 | 降频到可收敛的频率 |
| STA | 多 corner 全部通过后又新增违例 (ECO 引入) | BLOCKING | 撤销最后的 ECO；分析 ECO 副作用 | 上一步 ECO 之前 | 1 | 改为手工 ECO 或接受当前结果 |

**综合阶段 WNS < 0 诊断子表**（synth→iSTA 后发现 WNS 为负时，Agent 必须先排查以下配置问题，再判断是否为设计本身的问题）：

| 诊断项 | 检查方式 | 如果是这个原因 | 修复方式 |
|--------|---------|--------------|---------|
| 综合和 STA 用了同一个 corner 的 .lib 吗？ | 对比 synth 命令中的 LIB 和 STA 命令中的 LIB 路径 | 不一致 → 综合用 TYP 库跑出门电路，STA 用 SLOW 库算时序，当然对不上 | 统一 corner，重跑 |
| I/O 约束是否过于苛刻？ | 检查 SDC 中 `set_input_delay` / `set_output_delay` 的值 | 设了 0.1ns 的外部延迟而实际外部设备需要 0.8ns → STA 给 input path 留的余量太少 | 修正为实际值，重跑 |
| `clock_uncertainty` 是否过于保守？ | 检查 SDC 中 `set_clock_uncertainty` 的值 | 设了 0.5ns 而实际时钟抖动只有 0.1ns → 多留了 0.4ns 安全税，吃掉了逻辑的 slack | 按实际时钟质量修正，重跑 |
| 综合 effort level 是否拉满？ | 检查 Yosys 命令中的 effort 参数 | 用了低 effort → 综合器没尽力优化，逻辑级数过多 | 提高 effort，重跑 |
| 是否用了不匹配的 PVT 库？ | 检查 .lib 文件的 PVT 条件是否和设计目标一致 | 目标 1.8V 但用了 1.6V 的库 → 晶体管在你需要的电压下比库描述的慢 | 换成匹配的 .lib，重跑 |

**诊断结论**：

```
诊断子表全部排除后:
  WNS 在 WLM 可解释范围内 (如 -0.3ns 以内)
    → 继续物理实现 (WLM 的悲观会被实际布局修复)
  WNS 严重偏离 (如 -5ns)
    → 这才大概率是设计本身的问题
    → Agent 建议: 降频 / 改 RTL 架构 / 换更快的库
```

#### 6.5.2 收敛循环决策树

**定位**：活动 2（收敛循环）的核心逻辑。在 6.3 的 Solution 生命周期中，活动 2 被触发后，Agent 按此决策树运行。

**输入**：活动 1（执行 Flow）产出的 GDS + 问题清单（DRC 违例 / Timing 违例 / 面积功耗超标 / IR-drop）。

```
输入: 活动 1 的输出 (GDS + 问题清单)
      │
      ▼
┌─────────────────────────────────┐
│ 分类问题:                        │
│  · DRC 违例 (数量、类型、位置)    │
│  · Timing 违例 (WNS/TNS/路径)   │
│  · 面积/功耗 超标               │
│  · IR-drop 超标 (如有数据)       │
└────────────┬────────────────────┘
             │
    ┌────────▼────────┐
    │ 问题之间是否关联？ │
    └──┬──────────┬───┘
       │ 独立      │ 关联（修A会影响B）
       ▼           ▼
  逐个修复      联合修复
  (单步增量)    (多步回溯)
       │           │
       └─────┬─────┘
             ▼
    ┌─────────────────┐
    │ 从回溯点开始重跑  │
    │ 重跑到 GDS       │
    │ 重新检查所有指标  │
    └────────┬────────┘
             │
    ┌────────▼────────┐
    │ 所有指标通过?     │
    │ Yes → 进入活动4  │
    │ No  → 继续活动3  │
    │ 连续N轮无改善 →  │
    │    止损 (见下方)  │
    └─────────────────┘
```

**止损策略**（连续多轮无改善时，Agent 不再继续死磕）：

| 止损条件 | Agent 建议 |
|---------|-----------|
| DRC 连续 5 轮无改善 | 建议检查 LEF/工艺规则映射；可能需更换 PDK |
| Timing WNS 连续 3 轮恶化 | 停止优化；建议降频 20% 或切换到更快的库 |
| 面积连续膨胀超预算 50% | 建议降低利用率或增大 die；评估设计分区 |
| 单轮耗时超过预期 5 倍 | 建议检查约束是否合理；可能工具卡在死循环 |
| 收敛循环总轮数 > 20 | 建议重新评估设计目标是否合理；考虑换策略（换 PDK / 换综合策略 / 降频） |

### 6.6 Sheet 5: Scenario_Examples（5 个场景的 Flow 拼装结果）

**定位**：展示 Sheet 2（生命周期）+ Sheet 3（拼装规则）应用到不同场景后，产出的 Flow 长什么样。**不重复 Sheet 4 的收敛逻辑**。

5 个场景的差异集中在三个参数上：截断点、跳过规则、强度参数。同一套规则，不同输入，不同输出。

| 场景 | 截断点 | 跳过的能力 | STA corner | 覆盖率 | 综合策略 | 最终 Flow（以 GCD 为例） |
|------|--------|-----------|-----------|--------|---------|------------------------|
| **高中生体验** | 仿真 | 综合及以后全部 | 0 (不跑 STA) | — | — | Verible lint → Verilator compile → Icarus 仿真 → 看波形 |
| **本科课程** | 仿真+覆盖率 | 综合及以后全部 | 0 | ≥60% | — | 上面 + AI 生成 testbench → 覆盖率收集 → Bug 面板 |
| **竞赛 PPA 对比** | 综合+STA | CDC(单时钟), LP(无UPF), DFT, 物理全跳 | 2 (TYP+SLOW) | — | 面积优化+时序优化对比 | synth→iSTA(TYP摸底)→synth(面积)→iSTA(TYP)→synth(时序)→iSTA(TYP+SLOW) |
| **科研评估** | 物理设计+STA | DFT, ATPG, LVS (可选) | 1-3 (由研究目标决定) | 可选 | 默认 | synth→iSTA→floorplan→placement→CTS→routing→iSTA→iDRC→GDS |
| **流片签核** | 全流程 GDS | 无 (全部执行) | 3+ (TYP+SLOW+FAST+SI) | ≥95% | 多策略竞优 | 全部 55 项能力按顺序执行，活动 3 迭代到 clean |

**同一场景下不同设计的 Flow 差异示例**（展示跳过规则的威力）：

| 设计特征 | 影响 | 示例 |
|---------|------|------|
| 单时钟 GCD | 跳过 CDC、RDC | 流片 Flow 少 2 步 |
| 无 UPF 的 AES | 跳过低功耗检查 | 流片 Flow 少 3 步 |
| 纯组合逻辑乘法器 | 跳过 CTS、optHold | 物理 Flow 少 2 步 |
| 含 PLL IP 的 SoC | 增加 IP 模型检查 | 准入 Flow 多 2 步 |

**关键**：这些 Flow 不是人工预设的——是 Agent 拿到场景参数 + 设计特征后，用 Sheet 3 的规则从 Sheet 1 的能力池中**自动拼装**出来的。场景变了，Flow 就变；设计特征变了，Flow 也变。

---

## 七、所有修改意见汇总

### A. Excel 文档修改

| # | 内容 | 对应 Sheet |
|---|------|-----------|
| A1 | 新增阶段 0 (RTL生成) 的能力项 | Sheet 1 |
| A2 | 原 7 阶段所有步骤打散为原子能力 | Sheet 1 |
| A3 | Agent 四维决策规则 | Sheet 2 |
| A4 | 5 个典型场景的 Flow 实例 | Sheet 3 |
| A5 | 卡住信号 + 应对策略 + 跨阶段回退规则 | Sheet 4 |
| A6 | 三层迭代模型 + 收敛策略 + 止损规则 | Sheet 5 |
| A7 | 工具链全局替换 (iEDA 替代 OpenROAD/商业工具) | Sheet 1-3 |
| A8 | 通关标准改为铜/银/金三级 | Sheet 1 |

### B. 概念框架

| # | 内容 |
|---|------|
| B1 | Flow = 最小完整决策单元; Solution = 多 Flow 按目标组合; Stage = 管理分组 |
| B2 | "5 次 Flow" → 3 种活动: 执行 Flow(1次, 内含 synch→iSTA 闸门检查) + 收敛循环(N次) + 归档交付(视场景) |
| B3 | 每阶段执行前加入"摸底探测" (10-30 秒快速判断哪些检查适用) |

### C. 收敛策略（新文档）

| # | 内容 |
|---|------|
| C1 | 三层迭代模型: L1 阶段内 / L2 跨阶段回退 / L3 多 corner 收敛 |
| C2 | 跨阶段回退规则表 (卡住信号 → 回溯目标 → 改什么参数 → 止损阈值) |
| C3 | 止损策略 (连续 N 轮无改善 → 建议降频/换策略/改设计) |
| C4 | Agent Runtime 数据模型 (每轮输入参数 + 输出指标 + 违例分布) |

### D. ic_agent_os 代码层（按四模块组织）

| # | 模块 | 改动 | 文件 | 说明 |
|---|------|------|------|------|
| D1 | **Adapter** | 新增 Verible runner | `adapter/verible_runner.py` | subprocess 调 CLI |
| D2 | **Adapter** | 新增 Icarus runner | `adapter/icarus_runner.py` | subprocess + VCD 解析 |
| D3 | **Adapter** | 新增 Verilator runner | `adapter/verilator_runner.py` | subprocess + C++ 编译 |
| D4 | **Adapter** | 新增 SymbiYosys runner | `adapter/sby_runner.py` | .sby 生成 + solver 输出解析 |
| D5 | **Adapter** | 新增 Netgen runner | `adapter/netgen_runner.py` | LVS 脚本驱动 |
| D6 | **Adapter** | 新增 ChipMATE runner | `adapter/chipmate_runner.py` | vLLM/API 双后端 |
| D7 | **Adapter** | iEDA runner 扩展 | `adapter/ieda_runner.py` | 确保支持全流程 + iDRC + iIR |
| D8 | **Adapter** | 工具配置扩展 | `adapter/config.yaml` | 新增 6 个新 runner 的配置段 |
| D9 | **State** | 轨迹表扩展 | `state.py` | 新增 `violations` (DRC/Timing 违例分布) 和 `stage_metrics` (每阶段指标) 字段，支持收敛历史图数据源 |
| D10 | **Optimizer** | 收敛循环集成 | `tools/bayes_opt.py` 扩展 | Optimizer 在活动 2 每轮中提议最优参数修改量（如 density 降多少），替代盲目 ±5% |
| D11 | **Evaluator** | Flow 可行性预判 | `tools/` 或新建 | 根据相似设计的历史数据，预测某组参数跑 physical flow 的成功概率 |
| D12 | — | 新增 FastAPI 后端 | `server/` (新建目录) | REST + WebSocket，对接四模块 + Excel 5 Sheet 规则引擎 |
| D13 | — | Replanner 阶段名修复 | `composer/replanner.py` | Bug fix（`placement`→`gplace/resize/dplace` 等） |

### E. 前端（新建 `frontend/` 项目）

| # | 组件 | 说明 |
|---|------|------|
| E1 | 总体布局 Shell | 左导航 + 中央 + 右面板 + 底部状态栏 |
| E2 | 阶段 1 主页 | 三 Tab + Monaco + 波形面板 |
| E3 | 阶段 2 主页 | 三维度验证 + Testbench 编辑器 + Bug 追踪 + 覆盖率面板 |
| E4 | 阶段 3 主页 | Solution 配置 + Flow 实时可视化 + 指标面板 + GDS 预览 + 收敛历史图 |
| E5 | 对比实验配置页 | 变量配置 + 组合列表 + 实验进度 |
| E6 | 对比实验结果页 | 横向总表 + 柱状图 + 空间 Map 网格 + 单设计详情 |
| E7 | 右侧辅助面板 | 知识卡片 + 通关进度 + Agent 反馈流 + 操作历史 |
| E8 | 波形组件 | vcdrom (轻量) + Surfer WASM (深度) |
| E9 | GDS 预览组件 | KLayout PNG 瓦片 + OpenSeaDragon |

### F. 部署与基础设施

| # | 内容 |
|---|------|
| F1 | Docker Compose: 前端 + API + AI(vLLM) + 仿真(Icarus/Verilator/Verible/Surfer) + 综合(Yosys/SymbiYosys) + 物理验证(iEDA/Netgen) |
| F2 | 共享数据卷: `/workspace/{user_id}/` 结构 (rtl/ / constraint/ / netlist/ / reports/ / gds/) |
| F3 | WebSocket 事件协议: step_start / step_done / log / metric_update |
| F4 | 用户认证: JWT + SQLite |

---

## 八、与会议纪要的逐条对齐

| 会议要求 | 本方案对应 | 状态 |
|---------|-----------|------|
| 双模式服务（上传+AI生成） | 阶段1 三Tab（AI生成/手写/上传） | ✅ |
| Docker + Verilator 仿真环境 | 仿真容器（Icarus+Verilator双引擎） | ✅ |
| 前端波形展示 + 运行报告 | 阶段1/2 波形面板 + 各阶段报告按钮 | ✅ |
| 三维度验证（debug/仿真/形式化） | 阶段2 三维度勾选 + Bug追踪 + 覆盖率 + SVA | ✅ |
| 开源 coding agent + 大模型 | ChipMATE + DeepSeek API | ✅ |
| 暂不接入商业付费工具 | 全链路开源工具（iEDA/Yosys/Verilator...） | ✅ |
| 优先复用开源能力 | 基于 iFlow + ic_agent_os + iEDA 现有代码 | ✅ |
| 基于 Silicon/I/Open flow 适配 | 阶段3 基于 iEDA flow (iFP→iPL→iCTS→iTO→iRT) | ✅ |
| 动态生成定制化 flow 的 agent | Agent 三层决策（L1选择/L2迭代/L3回退） | ✅ |
| 面向学习者的交互设计 | 关卡系统 + 铜银金通关 + 知识卡片 + Agent 解释 | ✅ |
| 游戏化反馈机制 | 通关进度条 + 徽章 + 收敛历史可视化 + 对比实验排名 | ✅ |

---

## 九、澄清与常见疑问

### 9.1 ChipMATE 模型规格说明（纠正此前错误）

ChipMATE 实际发布的模型只有 **4B 和 9B** 两个规格，不存在 32B 版本：

| 模型 | HuggingFace ID | 用途 |
|------|---------------|------|
| ChipMATE-V-4B | `core12345/ChipMATE-V-4B` | Verilog 生成 Agent (轻量) |
| ChipMATE-P-4B | `core12345/ChipMATE-P-4B` | Python 参考模型 Agent (轻量) |
| ChipMATE-V-9B | `core12345/ChipMATE-V-9B` | Verilog 生成 Agent (推荐) |
| ChipMATE-P-9B | `core12345/ChipMATE-P-9B` | Python 参考模型 Agent (推荐) |

9B 是最大规格，已超过 GPT-4o 在 VerilogEval V2 上的表现（80.1% vs 64.1%）。如果需要比 9B 更强的 RTL 生成能力，途径是：ChipMATE 框架 + DeepSeek-V4 API 作为后端（ChipMATE 原生支持 OpenAI 兼容 API 后端），而非更大的 ChipMATE 模型。

### 9.2 为什么不用"伪逻辑"这个词

文档 6.4 节中的"Agent 决策流程"描述的是 Agent 如何根据场景、设计特征、可用资源来拼装 Flow 的**推理步骤**。它不是伪代码（pseudocode），也不是假逻辑——而是 Agent 真正要执行的决策过程。用"伪逻辑"这个词容易造成误解。已修正为"Agent 决策流程"。

### 9.3 5 张 Sheet 之间的关系

此前文档中 Sheet 3 (Agent_Decision) 和 Sheet 4 (Exception_Handling) 各自独立描述，读者容易感到它们互不关联。实际关系如下：

```
                         Sheet 2: Solution_Lifecycle
                         顶层调度器："现在该干什么？"
                                  │
          ┌───────────────────────┼───────────────────────┐
          │                       │                       │
          ▼                       ▼                       ▼
     活动 1: 执行 Flow        活动 2: 收敛循环        活动 3: 归档
          │                       │                       │
          │                每轮:                    用 Sheet 3
          │                ① Sheet 4 诊断          拼装签核 Flow
          │                ② Sheet 4 决策
          │                ③ Sheet 3+1 重拼装
          │                       │
          │                       ▼
          ▼                 Sheet 4 止损条件
    跑完后整体诊断:           (连续N轮无改善)
    Sheet 1 验收标准
    + Sheet 4 卡住信号
```

**5 张 Sheet 的角色分工**：

| Sheet | 角色 | 被谁使用 | 使用时机 |
|-------|------|---------|---------|
| Sheet 1: Capability_Pool | 原料库 + 验收标准 | Sheet 3 查询能力项 | Flow 拼装时 |
| Sheet 2: Solution_Lifecycle | **顶层调度器** | — | 整个 Solution 生命周期 |
| Sheet 3: Agent_Decision | 规划工具 | 活动 1(初始拼装) + 活动 2 回溯后(重拼装) + 活动 3(签核拼装) | 需要拼装 Flow 时 |
| Sheet 4: Exception_Handling | 诊断工具 | 活动 1 跑完后(整体诊断) + 活动 2 每轮(诊断+决策) | 跑完一批步骤后检查结果 |
| Sheet 5: Scenario_Examples | 演示 | 人类阅读 | 理解系统如何工作 |

**两层闸门的区别**（关键澄清）：

| 闸门 | 触发时机 | 检查什么 | 使用的标准 | 不通过怎么办 |
|------|---------|---------|-----------|------------|
| **步进闸门** | 活动 1 每步执行后 | 产出物是否可用？文件存在、非空、无 Fatal Error | 基础校验（非 Sheet 1 铜标准） | 中断活动 1，进入活动 2 |
| **整体诊断** | 活动 1 跑完后（或中断后） | 整体指标是否可接受？WNS、DRC、面积是否在预算内 | Sheet 1 铜/银/金标准 + Sheet 4 卡住信号 | 决定进入活动 2 还是活动 3 |

两者协同工作的完整流程：

```
用户需求
  │
  ▼
┌─────────────────────┐
│ Agent_Decision       │  ← 活动 1 开始: 拼装 Flow
│ (Sheet 3)            │
│ "根据场景选能力"     │
└────────┬────────────┘
         │
         ▼
┌─────────────────────┐
│ 逐步执行 Flow         │
│ 每步后: 步进闸门检查  │  ← 文件存在? 非空? 无 Fatal?
│ (基础校验)            │
└────────┬────────────┘
         │
         ▼
┌─────────────────────┐
│ 整体诊断              │  ← 活动 1 跑完后
│ (Sheet 4 + Sheet 1)  │
│ WNS≥0? DRC=0?       │
└────┬──────────┬─────┘
     │ 通过       │ 不通过
     ▼            ▼
  进入活动 3   ┌──────────────────────┐
              │ Exception_Handling    │  ← 活动 2 每轮
              │ (Sheet 4)             │
              │ "独立修还是联合回溯？"  │
              └────────┬─────────────┘
                       │
                       ▼
               确定回溯目标
                       │
                       ▼
               Sheet 3 + Sheet 1
               从回溯点重拼装 Flow
                       │
                       ▼
               重新执行 ──→ 再次诊断 ──→ ...
                                        │
                                 全部通过 → 活动 3
```

### 9.4 ic_agent_os 与实训平台的关系

**ic_agent_os 是一个完整的四模块 Agent 操作系统，实训平台是构建在其上的 IC 设计教育应用。**

#### 9.4.1 ic_agent_os 的完整架构（四大模块 + SkillOpt）

```
┌─────────── OpenCode Agent Runtime (TS/Bun) ───────────┐
│  @ic-agent-os/opencode-plugin : tool() + hooks + MCP   │
└──────────────────────┬────────────────────────────────┘
                       │ tool 调用 / MCP
┌──────────────────────▼────────────────────────────────┐
│  IC-Agent-OS Core (纯 Python)                          │
│                                                       │
│  ┌──────────┐  ┌───────────┐  ┌──────────┐  ┌───────┐ │
│  │ Adapter  │→ │ Evaluator │→ │Optimizer │→ │ State │ │
│  │ 跑真值   │  │ 代理预筛  │  │ 提案参数 │  │轨迹/  │ │
│  │          │  │           │  │          │  │Diff/  │ │
│  │          │  │           │  │          │  │知识   │ │
│  └────┬─────┘  └─────┬─────┘  └────┬─────┘  └───┬───┘ │
│       │              │             │            │      │
│       └──────────────┴──────┬──────┴────────────┘      │
│                             │                          │
│                      + SkillOpt                        │
│                   调度 / 进化 / 反思                     │
└─────────────────────────────┬──────────────────────────┘
                              │
┌─────────────────────────────▼──────────────────────────┐
│  EDA 执行层（可插拔后端）                                 │
│  模拟: ngspice + AnalogGym + sky130                     │
│  数字: Yosys/pyosys → iSTA(via ieda_py)                │
│  统一封装: AiEDA + iEDA MCP server                      │
└────────────────────────────────────────────────────────┘
```

| 模块 | 职责 | 实训平台如何使用 |
|------|------|----------------|
| **Adapter** | 统一 EDA 接入——输入设计+参数 → 输出 `StructuredMetrics` 或 `SimError`。**系统唯一接触真值的地方** | **直接复用**。现有 Yosys/iEDA/iSTA runner 零改动。新增 Verible/Icarus/Verilator/SymbiYosys/Netgen/ChipMATE runner 遵循现有 `contract.py` 接口 |
| **Optimizer** | 混合变量黑箱优化——Random→BO→LLM-Guided→Hybrid。输入搜索空间+约束 → 输出参数提案。**不内置电路知识** | **驱动收敛循环**。当 Agent 决定"退回到 placement，降低 density"，Optimizer 根据历史轨迹（来自 State）提出最优 density 值，而非盲目减 5%。竞赛场景中 Optimizer 自动探索 PPA 最优参数组合 |
| **State** | 系统记忆——L1 SnapshotStore（每步入库）、L2 DeltaDiff（因果归因："改了 X→Y 变了多少"）、L3 KnowledgeExtractor（提炼规律→`KnowledgeHint`） | **驱动整个 Agent 决策**。每轮 Flow 的 params/metrics/violations 存入 State → 收敛历史图的数据源 → Diff 归因告诉 Agent"上次降 density 后 DRC 减少了多少" → KnowledgeHint 给 Optimizer 做 warm-start |
| **Evaluator** | 代理预筛——在 Optimizer 提案后、Adapter 跑真值前，快速预估指标（~1ms/个），减少无效 Adapter 调用。**永不挡路，只省时间** | **优化摸底和收敛效率**。Evaluator 可根据相似设计的历史数据，预测"这套参数跑 physical flow 大概率失败"，建议 Agent 跳过或调整。不确定性高时仍走 Adapter |
| **SkillOpt** | Skill 进化循环——Rollout→Reflect→Edit→Gate | **关卡系统自我进化**。随着学生使用数据积累，SkillOpt 优化通关标准、知识卡片内容、Agent 建议策略 |

#### 9.4.2 数据闭环：四模块在实训平台中的协作

```
用户在阶段 3 点击"开始"
        │
        ▼
┌──────────────────────────────────────────────────┐
│  SkillOpt: 根据场景加载 Skill (竞赛PPA / 流片签核) │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────┐
│  Optimizer: 提议初始参数                           │
│  (density, clock_period, effort level...)         │
│  来源: State 的 KnowledgeHint (相似设计的历史最优)   │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────┐
│  Evaluator: 快速预估这组参数的效果                   │
│  PROMISING → 继续                                  │
│  POOR → 跳过, 让 Optimizer 重新提议                 │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────┐
│  Adapter: 跑真实 EDA 工具                          │
│  Yosys synth → iSTA → iEDA floorplan → ... → GDS  │
│  返回: StructuredMetrics (WNS/area/power/DRC)     │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────┐
│  State: 入库                                        │
│  Snapshot(params, metrics, violations, cost)       │
│  Diff: 本轮的 WNS 比上一轮改善了 +0.3ns             │
│    归因: density 降低 5% → DRC -30%                │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
          ┌──────────┴──────────┐
          │                     │
     指标 clean?            指标不通过
          │                     │
          ▼                     ▼
   进入归档/结束          回到 Optimizer
                         (下一轮提案)
```

#### 9.4.3 代码层面：实训平台新增什么、复用

```
ic_agent_os (现有 @ /home/xu/ic_agent_os)    实训平台 (新建)
══════════════════════════════════════════    ════════════════

adapter/ (Adapter 模块)                     复用全部 + 新增 6 个 runner
├── adapter.py                              ├── verible_runner.py
├── digital_runner.py (Yosys)               ├── icarus_runner.py
├── ieda_runner.py (iEDA + iSTA)            ├── verilator_runner.py
├── opensta_runner.py                       ├── sby_runner.py
├── contract.py (接口契约 v1.0)             ├── netgen_runner.py
└── snapshot_builder.py                     └── chipmate_runner.py

tools/ (Optimizer 模块)                     复用
├── bayes_opt.py (Optuna BO)
└── ista_verification.py

state.py (State 模块, SQLite 8 表)         复用

composer/ (Flow 引擎)                       复用 + 扩展
├── flow_composer.py                       新增 FlowPhase 枚举
├── tool_registry.py                       + Agent_Decision 规则引擎
├── goals.py (PPASpec)
└── replanner.py (L0-L3 代价分级)

cli.py (终端交互)                           server/ (新建, FastAPI + WebSocket)
                                            frontend/ (新建, Next.js)

无 AI 模型集成                              AI 容器 (ChipMATE + DeepSeek-V4)
无 Docker 编排                              Docker Compose (6 容器)
无教育功能                                  关卡系统 / 知识卡片 / 通关进度
```

**关键**：实训平台不是替代 ic_agent_os，而是给它加了一个**Web 界面 + IC 教育场景的领域知识层**。ic_agent_os 的四模块（Adapter/Optimizer/State/Evaluator）作为核心引擎继续运行，实训平台新增的 Excel 5 Sheet（Capability_Pool/Agent_Decision/Exception_Handling/Scenario_Examples/Solution_Lifecycle）是配置在这个引擎上的**IC 设计教育领域的决策规则**。

### 9.5 这个方案是不是构建了一个 WebIDE？

**是的，本质上是面向 IC 设计教育的专用 WebIDE，底层运行在 ic_agent_os 四模块引擎上。**

| 通用 WebIDE | 本方案 |
|------------|--------|
| VS Code Server / GitHub Codespaces | → 阶段 1 的 Monaco 代码编辑器 |
| 编译器（gcc/clang） | → Verilator / Yosys |
| 调试器（gdb） | → Icarus + Surfer 波形调试 |
| CI/CD Pipeline | → iEDA 物理设计 Flow（Adapter 模块驱动） |
| GitHub Copilot | → ChipMATE RTL 生成 + Agent 决策 |
| 代码审查 | → Bug 追踪面板 + Agent 反馈 |
| 性能 Profiler | → 收敛历史图（State 模块 Diff 数据）+ 对比实验 |
| **无对应** | → **Optimizer 驱动的收敛循环**（非通用 IDE 能力） |
| **无对应** | → **Evaluator 代理预筛**（节省昂贵的物理实现时间） |
| **无对应** | → **State 因果归因**（"改了什么参数→指标怎么变"） |

但比通用 WebIDE 多了三个专属能力：

| 通用 WebIDE 不具备 | 本方案 |
|-------------------|--------|
| ❌ 芯片物理设计后端 | ✅ iEDA (floorplan→GDS) + GDS 预览 |
| ❌ EDA 工具链集成 | ✅ 全链路开源 EDA（仿真→综合→STA→物理→DRC→LVS） |
| ❌ IC 设计教育场景 | ✅ 关卡系统 + 铜银金通关 + 知识卡片 + 收敛可视化 + 多 PDK 对比实验 |

---

## 十、实施计划

### 10.1 总览

```
Phase 0: 已就绪（零工作量）
Phase 1: 工具链补齐（2 周）
Phase 2: Adapter 扩展（2 周）
Phase 3: 后端 API + Agent 引擎（2 周）
Phase 4: 前端（3 周）
Phase 5: 教育功能 + 文档收口（1 周）
──────────────────────────────────
总计: ~10 周到可演示初版
```

### 10.2 各阶段详情

#### Phase 0 — 已就绪

| 项 | 状态 |
|----|------|
| ic_agent_os 四模块代码（Adapter / Optimizer / State / Evaluator） | ✅ |
| Adapter 现有 runner：Yosys / iEDA / iSTA | ✅ |
| iFlow RTL 设计集：gcd / aes_cipher_top / uart | ✅ |
| iEDA 物理设计全流程 + iDRC | ✅ |
| ChipMATE 框架选型确认（4B / 9B 模型） | ✅ |
| 方案文档 v3 定稿 | ✅ |

#### Phase 1 — 工具链补齐（W1–W2）

**W1：Docker 镜像构建**

| 镜像 | 包含工具 | 用途 |
|------|---------|------|
| 仿真镜像 | Icarus Verilog + Verilator + Verible + Surfer | RTL 编译、仿真、波形 |
| 综合镜像 | Yosys + SymbiYosys | 逻辑综合、形式验证 |
| 物理验证镜像 | Netgen | LVS |

**W2：ChipMATE 集成验证**

| 步骤 | 内容 |
|------|------|
| 下载模型 | ChipMATE-V-9B + ChipMATE-P-9B（HuggingFace） |
| 部署 vLLM | 双 Agent 分别在 GPU 8001/8002 端口 |
| 最小闭环 | NL 需求 → ChipMATE 生成 RTL → Icarus 交叉验证 → 输出匹配率 |

#### Phase 2 — Adapter 扩展（W3–W4）

**W3：新增 6 个 adapter runner**

| 文件 | 工具 | 调用方式 | 预估行数 |
|------|------|---------|---------|
| `adapter/verible_runner.py` | Verible | subprocess CLI | ~50 |
| `adapter/icarus_runner.py` | Icarus Verilog | subprocess + VCD 解析 | ~80 |
| `adapter/verilator_runner.py` | Verilator | subprocess + C++ 编译 | ~100 |
| `adapter/sby_runner.py` | SymbiYosys | .sby 配置生成 + solver 输出解析 | ~120 |
| `adapter/netgen_runner.py` | Netgen | subprocess LVS 脚本 | ~60 |
| `adapter/chipmate_runner.py` | ChipMATE | HTTP API（vLLM / DeepSeek 双后端） | ~150 |

**W4：配置扩展 + 联调**

| 步骤 | 内容 |
|------|------|
| `adapter/config.yaml` 扩展 | 新增 6 个 runner 的路径、超时、PDK 配置段 |
| 单 runner 验证 | 每个 runner 独立跑通（Verible lint GCD → Icarus 仿真 GCD → …） |
| 全量联调 | 6 个新 runner + 3 个旧 runner 全链路跑通 |

#### Phase 3 — 后端 API + Agent 引擎（W5–W6）

**W5：FastAPI 后端搭建**

| 组件 | 内容 | 预估行数 |
|------|------|---------|
| REST API | Flow 编排（compose / run / status / history）+ 用户管理 + 文件管理 | ~300 |
| WebSocket | 步骤实时推送（step_start / step_done / log / metric_update） | ~150 |
| 用户认证 | JWT + SQLite | ~100 |

**W6：Agent 决策引擎集成**

| 步骤 | 内容 |
|------|------|
| Excel 5 Sheet 规则 → Python | Capability_Pool（dict）、Agent_Decision（4 维规则函数）、Exception_Handling（卡住信号查找表）、Solution_Lifecycle（状态机） |
| DeepSeek-V4 API 对接 | Agent 决策 Prompt 模板 + 教育交互 Prompt 模板 |
| State 轨迹表扩展 | `state.py` 新增 `violations` 和 `stage_metrics` 字段 |
| Optimizer 收敛集成 | 活动 2 每轮调用 Optimizer 提议参数修改量 |
| Evaluator 集成（可选） | 根据相似设计历史预判 Flow 可行性 |

#### Phase 4 — 前端（W7–W9）

**W7：项目搭建 + 阶段 1**

| 组件 | 内容 |
|------|------|
| Next.js 项目搭建 | TypeScript + Tailwind + 路由 |
| 总体布局 Shell | 左导航 + 中央工作区 + 右侧面板 + 底部状态栏 |
| 阶段 1 页面 | 三 Tab（AI生成 / 手写 / 上传）+ Monaco 编辑器 + Agent 反馈面板 |
| 波形组件 | vcdrom 嵌入（轻量）+ Surfer WASM 嵌入（深度） |

**W8：阶段 2 + 阶段 3**

| 组件 | 内容 |
|------|------|
| 阶段 2 页面 | 三维度验证勾选 + Testbench 编辑器 + 仿真控制 + Bug 追踪 + 覆盖率热力图 |
| 阶段 3 页面 | Solution 配置面板 + Flow 实时可视化（步骤状态机）+ 实时指标面板 |
| GDS 预览 | KLayout CLI PNG 瓦片 + OpenSeaDragon |
| 收敛历史图 | Recharts 折线图（WNS / 面积 / DRC 多轮变化） |

**W9：对比实验 + 联调**

| 组件 | 内容 |
|------|------|
| 对比实验配置页 | 变量配置 + 笛卡尔积组合列表 + 实验进度 |
| 对比实验结果页 | 横向总表 + 柱状图 + 空间 Map 网格 + 单设计详情（可展开） |
| WebSocket 联调 | 前端 ↔ FastAPI ↔ Docker 工具容器 实时消息通路验证 |
| 端到端测试 | 从 NL 需求到 GDS 产出，全部 3 阶段走通 |

#### Phase 5 — 教育功能 + 文档收口（W10）

| 步骤 | 内容 |
|------|------|
| 关卡系统 | 3 阶段 × 铜银金 = 9 关卡 + 通关条件 + 进度持久化 |
| 知识卡片 | 上下文感知切换 + 首批 20 张卡片（Setup/Hold/CDC/DRC/LVS…） |
| 铜银金通关 | Sheet 1 每个能力项的 pass_criteria 在前端可视化 |
| Excel 5 Sheet 文档 | 按方案第六节定稿 |

### 10.3 里程碑

| 里程碑 | 时间 | 验收标准 |
|--------|------|---------|
| **M0** | W0（起点） | 方案文档通过评审；ic_agent_os 现有代码可运行 |
| **M1** | W2 末 | ChipMATE 最小闭环跑通（NL→RTL→交叉验证）；3 个 Docker 镜像构建完成 |
| **M2** | W4 末 | 9 个 adapter runner 全部可独立调用并返回 StructuredMetrics |
| **M3** | W6 末 | FastAPI 后端可编排 Flow 并 WebSocket 推送步骤进度；Agent 决策引擎可生成裁剪后的 Flow |
| **M4** | W9 末 | 三阶段前端完整可用；端到端 GCD 从 NL→GDS 全链路跑通 |
| **M5** | W10 末 | 关卡系统 + 知识卡片上线；Excel 文档定稿；可对外演示 |

### 10.4 并行化策略

```
        工具链补齐          Adapter 扩展        后端+Agent          前端
W1: ████████████
W2: ████████████
W3:                    ████████████
W4:                    ████████████
W5:                                      ████████████
W6:                                      ████████████
W7:                                                        ████████████
W8:                                                        ████████████
W9:                                                        ████████████
W10: 教育功能 + 文档 ──────────────────────────────────────────────────██
```

W1–W6 严格串行（每一步依赖上一步产出）。W7–W9 前端可与 W5–W6 后端部分重叠——前端先用 mock 数据开发，后端 API 就绪后切换联调。W10 贯穿全程（文档和关卡内容可在开发过程中并行准备）。

W1–W6 严格串行（每一步依赖上一步产出）。W7–W9 前端可与 W5–W6 后端部分重叠——前端先用 mock 数据开发，后端 API 就绪后切换联调。W10 贯穿全程（文档和关卡内容可在开发过程中并行准备）。

---

## 十一、当前进度

### 11.1 Phase 1 — 工具链补齐 ✅ 完成

| 工具 | 版本 | 状态 |
|------|------|:---:|
| Icarus Verilog | 11.0 | ✅ |
| Verilator | 4.038 | ✅ |
| Yosys | 0.68+ (源码编译) | ✅ |
| Verible | v0.0-4126 | ✅ |
| SymbiYosys | 0.68 | ✅ |
| Netgen LVS | 1.5.323 | ✅ |
| Docker 文件 | Dockerfile×3 + compose | ✅ |

### 11.2 Phase 2 — Adapter 扩展 ✅ 完成

| Runner | 状态 | 测试 |
|--------|:---:|------|
| `icarus_runner.py` | ✅ | 编译+仿真真值测试通过 |
| `verilator_runner.py` | ✅ | lint 测试通过 |
| `verible_runner.py` | ✅ | lint+style 测试通过 |
| `sby_runner.py` | ✅ | BMC 引擎格式修正，Yosys 0.68 兼容 |
| `netgen_runner.py` | ✅ | 错误处理验证通过 |
| `chipmate_runner.py` | ✅ | API双后端，交叉验证匹配率100% |

### 11.3 Phase 3 — 后端 API ✅ 完成

| 端点 | 状态 |
|------|:---:|
| REST API (compose/run/status/history) | ✅ |
| WebSocket 实时推送 | ✅ |
| JWT 用户认证 | ✅ |
| AI 聊天 (`/api/chat`) | ✅ |
| Agent Decision 引擎 (`server/agent_engine.py`) | ✅ |
| SVA 模板库 (`server/sva_templates.py`) | ✅ |
| 对比实验执行器 (`server/experiment_runner.py`) | ✅ |
| 文件自动保存 (`/api/files/autosave`) | ✅ |
| API Key 持久化 (`server/settings.json`) | ✅ |

### 11.4 Phase 4 — 前端 ✅ 基本完成

| 页面 | 功能 | 状态 |
|------|------|:---:|
| 首页 | 仪表盘 + EDA工具状态 | ✅ |
| 阶段1 | Monaco编辑器 + AI生成/手写/上传 + Verible+Verilator+Icarus编译仿真 | ✅ |
| 阶段2 | 功能仿真(AI生成TB+SVG波形+采样点) + 形式验证(SVA模板+SBY)+AI聊天 | ✅ |
| 阶段3 | AI聊天Flow组装 + 执行 + 前置阶段状态检查 | ✅ |
| 对比实验 | 变量组合 + 批量执行 + 结果表 | ✅ |
| 右侧面板 | 📁文件浏览器 + 🤖Agent日志 + 📖信息 | ✅ |

### 11.5 方案能力池对照

#### 阶段 0（前端阶段1）：RTL 设计与生成

| ID | 能力 | 状态 | 备注 |
|----|------|:---:|------|
| RTL-001 | NL→RTL 生成 | ✅ | ChipMATE + DeepSeek API，交叉验证匹配率100% |
| RTL-002 | 代码手写 | ✅ | Monaco Editor，Verilog语法高亮 |
| RTL-003 | 文件上传 | ✅ | .v/.sv 文件上传 |
| RTL-004 | RTL 语法检查 | ✅ | Verible，显示违例数 |
| RTL-005 | RTL 编译 | ✅ | Verilator --lint-only |
| RTL-006 | 基础功能仿真 | ✅ | 无TB时自动生成激励（20组测试向量）|
| RTL-007 | 波形查看 | ⚠️ | SVG波形（方案规划Surfer/vcdrom，当前为自定义SVG实现） |
| — | 铜/银/金通关判定 | ❌ | 徽章硬编码，未根据实际结果动态判定 |

#### 阶段 2（前端阶段2）：仿真与验证

| ID | 能力 | 状态 | 备注 |
|----|------|:---:|------|
| QA-001 | Lint 代码规范检查 | ✅ | Verible |
| QA-002 | CDC 跨时钟域检查 | ❌ | 方案标注"开源无成熟工具"，待实现教育替代方案 |
| QA-003 | RDC 跨复位域检查 | ❌ | 同上 |
| QA-004 | 低功耗 UPF 检查 | ❌ | 方案标注"开源无"，待实现诚实标注方案 |
| QA-005 | SDC 约束质量检查 | ❌ | 待实现 |
| QA-006 | Formal Property Smoke | ⚠️ | SVA编辑器+SBY可用，但非自动化smoke |
| QA-007 | Smoke 仿真 | ✅ | Icarus + 用户/AI生成TB |
| QA-008 | 问题/Waiver 评审 | ❌ | 待实现 |
| — | 覆盖率可视化 | ❌ | Line/Branch/FSM/Toggle 覆盖率面板未实现 |
| — | Bug 追踪面板 | ❌ | Agent 未从 log 自动提取错误 |
| — | 仿真控制(断点/步进) | ❌ | 未实现 |
| — | 源码-波形联动 | ❌ | 未实现 |

### 11.6 待完善清单

| 优先级 | 内容 | 说明 |
|:---:|------|------|
| P0 | 阶段1 铜/银/金通关自动判定 | 根据 Verible/Verilator/Icarus 结果动态显示 |
| P0 | 阶段2 覆盖率可视化 | Line/Branch/FSM/Toggle 四项 |
| P1 | CDC/RDC 教育替代方案 | Agent 标注跨域路径 + 引导写同步器 |
| P1 | UPF/DFT/ATPG 诚实标注 | 标注"需商业工具" + 教育说明 |
| P1 | 波形升级 Surfer/vcdrom | 替代当前 SVG 方案 |
| P1 | 阶段2 Bug 追踪面板 | Agent 自动从仿真 log 提取错误 |
| P2 | 阶段3 GDS 预览 | KLayout PNG 瓦片 + OpenSeaDragon |
| P2 | 阶段3 收敛历史图 | WNS/面积/DRC 多轮趋势 |
| P2 | 仿真控制(断点/步进) | 阶段2 仿真面板 |
| P2 | 源码-波形双向联动 | 点击波形跳转源码行 |
