# 使用 IC-Agent-OS 完成全新芯片设计的完整流程

> 适用场景：拿到一个新的 RTL 设计，从零开始跑通全流程到 GDS2 交付

---

## 总览

```
                    第 0 步              第 1 步             第 2 步
                  环境就绪检查          探索轮               全流程轮
                 python3 setup_check  cli.py 交互选       自动应用历史建议
                      ~5s             ~2min synth+STA      ~10min 完整流程
                         │                │                     │
                         ▼                ▼                     ▼
                    ┌────────┐      ┌──────────────┐     ┌──────────────┐
                    │ 检查工具 │      │ 摸清设计体质   │     │ 产出 GDS2 +   │
                    │ 检查 PDK │      │ WNS? gates?   │────▶│ timing report │
                    │ 检查 RTL │      │ 存入历史数据库  │     │ DRC report    │
                    └────────┘      └──────────────┘     └──────────────┘
                                           │
                                    第 1.5 步（自动）
                                    record() + suggest_final()
                                    建议裁剪步骤 + 调整参数
```

---

## 第 0 步：环境就绪检查

```bash
cd ic_agent_os
python3 setup_check.py
```

检查项：

| 检查 | 要求 |
|------|------|
| Python ≥ 3.10 | ✅ 必须 |
| Yosys | ✅ 必须 (RTL 综合) |
| OpenROAD | ✅ 必须 (物理实现) |
| OpenSTA | ✅ 必须 (时序分析) |
| RTL 文件 | `rtl/` 目录下有设计文件 |
| PDK 库 | `config.yaml` 中路径可访问 |
| Python 包 | pyyaml, jinja2 |

全部 11 项通过才能继续。

---

## 第 1 步：交互式向导 → 探索轮

```bash
python3 cli.py
```

### 向导 7 步操作

| 步骤 | 做什么 | 第一次跑的推荐选择 |
|------|--------|------------------|
| ① 选择设计 | gcd / aes / uart / picorv32 / 自定义 | 选你的目标设计 |
| ② 选择工艺 | sky130 / ASAP7 / tsmc3 / tsmc5 / gf22 | sky130（有真实 PDK） |
| ③ 选择需求 | 开源 / 低功耗 / 面积 / 快速 / 签核 / AI训练 | 开源（默认） |
| ④ 设计目标 | 频率 MHz / 面积上限 μm² / 功耗上限 mW | **只填频率**（先摸底） |
| ⑤ 流程深度 | 完整 / 精简 | **精简**（先探索） |
| ⑥ 方案确认 | 查看 compose 生成的流程 | 回车确认 |
| ⑦ 执行模式 | 执行一遍 / 迭代优化 | 执行一遍 |

### 探索轮内部发生了什么

```
Step ①-④: 收集用户输入
  design=gcd, tech=sky130, requirements=["开源"]
  goals={frequency: 200}

Step ⑤: compose() 生成方案
  ├── FlowComposer 评分: Yosys(开源) vs DC(商业) → Yosys 胜
  ├── history.suggest_demo() → 冷启动无历史 → 返回默认参数
  └── 产出: 2 步 Flow (Yosys → OpenSTA)

Step ⑥-⑦: 执行
  第 1 轮: synthesis (Yosys) → STA (OpenSTA)
  结果: WNS=8.5ns, gate_count=640, passed=True

  ┌─ 历史反馈闭环（自动触发）─┐
  │ record() → 写入 run_history.db                 │
  │ suggest_final() → 分析结果, 给出建议              │
  │   推荐深度: 精简 (640gates + 8.5ns余量)         │
  │   建议跳过: filler                              │
  │   参数建议: 可提高 density                      │
  │ 重新 compose(history, diagnosis) → 第 2 轮用   │
  └─────────────────────────────────┘

  第 2 轮: 应用了第 1 轮建议的裁剪流程
    原 10 步 → 裁剪为 9 步 (跳过了 filler)
    → 全流程通过
    → WNS≥0 ✅  DRC report ✅
```

---

## 第 2 步（可选）：Sign-off 验证

全流程通过后，CLI 会询问是否跑多 corner STA：

```
Sign-off 轮 — 多 Corner STA
  TYP corner: 已跑 (第2轮)     WNS=8.31 ✅
  SLOW corner...               WNS=5.12 ✅  (1.6V, 125°C)
  FAST corner...               WNS=0.03 ✅  (2.0V, -40°C)
```

三个 corner 全部 WNS≥0 → Sign-off 通过 → 可以流片。

---

## 第 3 步：查看产出

```
outputs/
└── state/
    ├── state.db              ← 快照数据库 (每步执行结果)
    ├── run_history.db        ← 历史反馈数据库 (积累经验)
    └── snapshots/
        └── <snap_id>/
            ├── snapshot.json  ← 结构化快照
            └── artifacts/     ← 网表、DEF、timing.rpt

tmp/
├── digital_runs/<id>/output/GCD_synth.v   ← 门级网表
├── openroad_runs/<id>/output/
│   ├── floorplan.def / tapcell.def / pdn.def
│   ├── gplace.def / dplace.def / cts.def
│   ├── groute.def / droute.def
│   └── timing.rpt                          ← STA 报告
└── gds_runs/<id>/<design>.gds              ← 最终 GDS2
```

CLI 退出时会打印所有路径。

---

## 第 4 次跑（有历史积累后）

第二次跑同样的设计时：

```
suggest_demo()
  → 查 run_history.db
  → 找到历史记录: gcd/sky130/200MHz, WNS=8.5, passed=True
  → 返回: CLK_PERIOD=5.0ns (不用猜), PLACE_DENSITY=0.6 (验证过的)
  → compose() 直接用这些参数, 第一轮就能跑在最优配置上

suggest_final()
  → 数据更多, 统计更可靠
  → Yosys 100% → 评分满分
  → 多次历史全过 → 精简流程的信心更高
```

---

## 换个新设计的完整命令序列

```bash
# 1. 把新 RTL 放到 rtl/ 目录
cp /path/to/my_design.v rtl/
cp /path/to/my_design.sdc rtl/

# 2. 环境检查
python3 setup_check.py

# 3. 交互式启动
python3 cli.py
# 选 [0] 自定义设计 → 输入设计名 → 输入 RTL 路径 → 保存
# 选 sky130 → 选需求 → 填频率 → 选精简 → 执行

# 4. 第一次跑完，cli.py 自动做了:
#    - record() 入库
#    - suggest_final() 给建议
#    - 应用建议到全流程轮

# 5. 查看历史
python3 cli.py history

# 6. 第二次跑（有历史），同样命令，但:
#    - 初始参数自动优化
#    - 工具评分使用历史数据
#    - 流程深度自动推荐

# 7. 打包交付
#    产出: GDS2 + timing.rpt + DRC report + state.db
```

---

## 冷启动 vs 有历史 对比

| | 第一次跑 (冷启动) | 第 N 次跑 (有历史) |
|---|---|---|
| 初始 CLK_PERIOD | 默认 `1000/freq` | 历史上最优值 |
| 初始 density | 默认 0.6 | 历史上通过的最优值 |
| 工具选择 | 纯静态评分 | 静态 × 历史成功率 |
| 流程深度 | 用户手动选 | 自动推荐 |
| 步骤裁剪 | 无 | 自动跳过不必要的 step |
| 风险预警 | 无 | PDK/频率历史问题提前警告 |
| 跑完后 | record() 入库 | record() 入库 + 数据更多 |

---

## 关键设计决策

| 场景 | 第 ④ 步怎么选 | 第 ⑤ 步怎么选 | 为什么 |
|------|-------------|-------------|--------|
| 全新设计，不确定能跑多快 | 只填频率（宽松值） | 精简 | 先摸底，再决定 |
| 已有验证过的设计，要出 GDS | 填频率 + 面积 | 完整 | 需要物理实现数据 |
| 追求高频 (500MHz+) | 填频率 | 精简先试 → 再完整 | 先看 WNS 有没有余量 |
| 要流片 (tape-out) | 填频率 + 面积 + 功耗 | 完整 | 跑完后做 sign-off |
