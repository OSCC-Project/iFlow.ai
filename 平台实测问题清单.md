# 平台实测问题清单（不含波形专项）

> 测试日期：2026-08-16
> 测试方式：Windows Chrome 无头浏览器（CDP）+ API 层直接调用，覆盖首页 / 阶段1 / 阶段2（形式化验证）/ 阶段3 / 对比实验
> 说明：阶段2 波形渲染的专项问题（阶梯波形、轨道布局、tooltip、时间轴、采样点边界等）另文记录，本清单收录其余全部问题。

---

## 一、严重问题（P0 — 阻断主流程）

### P0-1 阶段3：AI 拼装 Flow 协议断裂，"▶ 执行"按钮永远不出现

**实测现象**：阶段3 聊天框发送"帮我做综合后PPA评估，快速"后等待 120 秒，Flow 面板和"▶ 执行"按钮始终不出现。直接调 `/api/chat` 确认：LLM 返回了完整的 PPA 分析文本（17.5s，deepseek-reasoner），但 `action=None`、`flow=None`。

**根因**：两层协议断裂：

1. [chat.py:63-72](server/chat.py#L63-L72) 的 `ChatSession.send()` 从回复中解析 `[ACTION:...]` 标记行来触发 Flow 拼装；
2. 但 [ai_knowledge.py](server/ai_knowledge.py) 的 `SYSTEM_PROMPT` **完全没有定义这个输出协议**——没有告诉 LLM "何时输出 [ACTION:...] 行、格式是什么"。模型永远不会输出一个它不知道的格式。

**影响**：阶段3 的核心入口（自然语言 → Agent 拼装 Flow）100% 失效。前端 [stage3/page.tsx:80](frontend/src/app/stage3/page.tsx#L80) `if (!d.flow) return` 也没有任何兜底，用户只能看到 AI 的文本分析。

**修复建议**：
- 在 `SYSTEM_PROMPT` 中明确定义 ACTION 协议（何时输出、格式、示例），例如：
  ```
  当用户明确要求跑 Flow（综合/物理实现/PPA/版图/流片）时，在你的回复末尾必须附加一行:
  [ACTION: target:ppa|gds|tapeout depth:quick|standard|signoff]
  ```
- 前端兜底：`d.flow` 为空时，提供手动场景选择按钮（体验/课程/竞赛/科研/流片）直接调 `/api/flow/compose`，不依赖 LLM。
- 可选：将 `/api/chat/compose` 已有的 `_keyword_parse` 兜底逻辑复用到 `/api/chat`。

### P0-2 阶段3：Yosys 综合必失败（TOP_MODULE 硬编码）

**实测现象**：research 场景跑物理流程，`yosys_synth` 步骤报错 `ERROR: Module 'top' not found!`（设计实际模块名是 `counter`）。

**根因**：[api.py:233](server/api.py#L233) `"TOP_MODULE": req.params.get("top_module", "top")` —— 硬编码默认 `"top"`，而前端 [stage3/page.tsx:107](frontend/src/app/stage3/page.tsx#L107) 调用时从不传 `top_module`。讽刺的是，[agent_engine.py:70-95](server/agent_engine.py#L70-L95) 的 `DesignProfile.from_code()` 已经有完整的 top_module 提取逻辑，但 [api.py:108](server/api.py#L108) compose 时用的是默认 `DesignProfile()`，从不分析 RTL。

**影响**：所有模块名不是 `top` 的设计（几乎全部）在综合步骤必然失败。

**修复建议**：
- `api_compose` 接收 `rtl_code` 参数，调用 `DesignProfile.from_code(rtl_code)` 生成真实设计画像（同时解决 P2-5）；
- `api_run` 的 `yosys_synth` / `sby_check` / iEDA 步骤统一使用 `flow["design"]["top_module"]`；
- 在 `DesignProfile.from_code()` 里复用端口解析能力（注意：与波形专项的端口解析 bug 同源，需一并修复）。

### P0-3 对比实验是空壳：不跑真实 Flow

**实测现象**：创建 `gcd × {sky130, nangate45}` 实验并运行，每个组合的结果只有 `lint_violations: 0` 和 0.002s 的 lint 耗时；`yosys_synth_duration: 0.0`、`ista_sta_duration: 0.0`。

**根因**：[api.py:835-860](server/api.py#L835-L860) `api_flow_run_internal()`：
1. `VeribleRunner({}).execute(design, {"rtl_files": [], "mode": "lint"})` —— **传的是空文件列表**，lint 没有 lint 任何东西（violations 恒 0）；
2. `yosys_synth` / `ista_sta` 被硬编码 `skipped`，reason 写死 "experiment mode: 仅 lint 步骤"。

**影响**：方案 5.2.4 要求"每个任务独立跑完整 Flow（Yosys 综合 → iEDA 物理设计）"、WebSocket 进度推送、横向总表/柱状图/空间 Map/单设计详情四区展示——当前后端是 stub，前端只有一张简单的 HTML 表格（见 P1-5），对比实验功能整体不可用。

**修复建议**：
- `api_flow_run_internal` 改为调用真实的 run 流程（复用 `api_run` 的步骤分发逻辑，抽成内部函数），RTL 按 design 名从 `/home/xu/iFlow/rtl/` 加载；
- 变量（PDK/利用率/频率）翻译为 iEDA/Yosys 参数后注入；
- 前端增加实验进度（组合级进度条）、柱状图（需安装 Recharts）、导出功能。

### P0-4 路由遮蔽：`/api/runs/history` 恒 404，收敛历史图永远不显示

**实测现象**：`GET /api/runs/history` 返回 `{"detail":"Not Found"}`，而 OpenAPI 里该路由确实存在。

**根因**：[api.py:448](server/api.py#L448) `@app.get("/api/runs/{run_id}")` 声明在 [api.py:527](server/api.py#L527) `@app.get("/api/runs/history")` **之前**。FastAPI 按注册顺序匹配路由，`history` 被当作 `run_id` 捕获 → 查不到该 run → 404。

**影响**：阶段3 前端 [stage3/page.tsx:127](frontend/src/app/stage3/page.tsx#L127) 调用后 `catch {}` 静默失败 → 收敛历史面板永不渲染。

**修复建议**：把 `/api/runs/history` 的声明移动到 `/api/runs/{run_id}` 之前（或改路径为 `/api/history/runs`）。

---

## 二、中等问题（P1）

### P1-1 阶段3：GDS 导出返回无关的旧文件

**实测现象**：本次 run 的设计是 `counter`，`gds_export` 步骤却返回 `GCD.gds`（历史遗留文件）。

**根因**：[api.py:287-300](server/api.py#L287-L300) gds_export 从 `/home/xu/ic_agent_os/tmp` **全局搜索最近修改的 .gds 文件**，与本次 run 的输出目录毫无关联。

**影响**：学生看到的是别人的版图。多用户/多次运行场景下必然错乱，教育场景中属于严重误导。

**修复建议**：从本次 run 的 iEDA 输出目录（`tmp/ieda_runs/{run_id}/`）取 GDS；没有则如实报 skipped，绝不返回无关文件。

### P1-2 阶段3：iEDA 各步骤"假成功"（跑的是内置用例，不是用户设计）

**实测现象**：research 场景下 `ieda_floorplan/place/cts/route/idrc_drc` 各步 0.4~0.6s 内"done"——对一个综合都失败了的流程，物理设计却能瞬间"成功"。

**根因**：Yosys 失败后 `rtl_paths[0]` 仍是用户 RTL（不是网表），iEDA runner 拿不到有效输入时回落到内置 `sky130_gcd` 测试用例（[ieda_runner.py](/home/xu/ic_agent_os/adapter/ieda_runner.py) 的脚本目录指向 iEDA 自带用例），跑的是 GCD 而不是用户设计。

**影响**：与 P1-1 叠加，阶段3 的"✅ 全流程完成"完全不可信。前端 [stage3/page.tsx:109](frontend/src/app/stage3/page.tsx#L109) 还把非 failed 的步骤一律显示为 done（skipped 也显示 ✅），进一步放大误导。

**修复建议**：
- 上游步骤失败时，下游 iEDA 步骤应直接 `skipped`（依赖未满足），禁止回落内置用例；
- 前端正确区分 `skipped`（⏭️）与 `done`（✅）——当前 `status: r.status==='failed'?'failed':'done'` 把 skipped 也标成 done；
- `ista_sta` 步骤被硬编码 skipped（[api.py:239-240](server/api.py#L239-L240)），应改为真正执行 iSTA 或如实标注。

### P1-3 形式化验证：SVA 模板对最简单的计数器也"证明失败"

**实测现象**：AI 生成 SVA（模板方式，3 条断言）→ SymbiYosys BMC 深度 10 → "❌ 证明失败 — 在深度 10 内找到反例"。

**根因**：[sva_templates.py:126-128](server/sva_templates.py#L126-L128) 模板生成的 `always @(posedge clk) if (rst_n && !en) assert (q == $past(q));` 在**首个时钟沿** `$past(q)` 为 X → 断言必然失败。反例是断言写法问题，不是设计问题。

**影响**：学生对任何带使能的计数器跑形式验证都会看到 FAIL，且无法区分"property 写错了"和"RTL 真有问题"——与方案"教育替代"的理念（引导学生理解验证而非困惑）相悖。

**修复建议**：
- 模板加初始保护，例如：
  ```systemverilog
  assert property (@(posedge clk) disable iff (!rst_n) (!en && $past(en) |-> q == $past(q)));
  ```
  （antecedent 含 `$past(en)`，首拍 antecedent 为 X 时 property 视为通过而非失败）；
- 或使用 `$past(q, 1, 0)` 并显式豁免首拍；
- 后端 `sby_check` 返回 verdict 时附带"失败的是哪条 property"（yosys-smtbmc trace 解析），前端"AI 分析"按钮已有入口，可自动带上 trace 摘要。

### P1-4 安全：所有 API 无鉴权 + 任意路径读写

**实测现象**：`GET /api/files/read?path=/etc/passwd` 直接返回文件内容；`POST /api/files/save` 可写任意路径。

**根因**：
1. `AuthManager`（注册/登录/JWT）已实现但**从未在任何路由上使用**；
2. [api.py:477-485](server/api.py#L477-L485) `api_files_read` / `api_files_save` 对 `path` 参数零校验；
3. 服务运行在 `0.0.0.0:8000`，局域网内任意机器可访问。

**影响**：实训平台部署到学校网络后，任何学生可读写服务器任意文件、查看他人数据。

**修复建议**：
- 为 `/api/**` 添加 JWT 依赖（前端补 Authorization header）；短期可先做路径白名单：`path` 必须位于 `/tmp/iflow_workspace/`、`/home/xu/ic_agent_os/tmp/` 等工作目录内（`os.path.realpath` 校验前缀）；
- 按方案 F2 的用户隔离模型（`/workspace/{user_id}/`），文件接口至少校验当前用户 ID 前缀。

### P1-5 对比实验前端：只有基础表格，方案 5.2.3 的四个展示区域缺失

**实测现象**：对比实验页只有"配置区 + 一张简单 HTML 表格"；实验运行期间无进度显示。

**与方案差距**：
| 方案要求（5.2.3） | 现状 |
|---|---|
| 区域 A 横向总表（列排序、行勾选、颜色编码、导出 CSV/Excel） | ❌ 只有静态表格 |
| 区域 B 关键横向柱状图（Die/Fmax/Power/DRC） | ❌ 无图表（**Recharts 甚至不在 package.json 依赖里**） |
| 区域 C 空间 Map 网格（统一色标热力图） | ❌ 无 |
| 区域 D 单设计详情（阶段表/版图/16 张空间 Map） | ❌ 无 |
| 区域 E 同工艺纵向结论（按 PDK 分组的变量扫描分析：完成率/最优配置/变化率/密度有效性判断/重复性检查，规则模板自动生成） | ❌ 无（2026-08-18 已补入方案 5.2.3 区域 E + 5.2.4 步骤 4，实现待做） |
| 实验进度（每个组合的进度条） | ❌ 无 |

**修复建议**：安装 Recharts；按 P0-3 打通真实数据后，按方案 5.2.3 的四个区域实现；空间 Map 按方案 5.2.6 用 matplotlib 批量渲染 CSV → PNG 网格。

### P1-6 WebSocket 实时推送仅阶段3 使用

**现状**：后端 ws.py 已完整实现（step_start/step_done/log/metric_update），但只有 [stage3/page.tsx:94](frontend/src/app/stage3/page.tsx#L94) 连接了 WS；阶段1/2 的"编译 & 仿真"、"自动激励仿真"都是同步 fetch 等待，运行期间页面无步骤级反馈。

**方案要求**（4.2）：全平台步骤实时推送，尤其阶段2 仿真和阶段3 长流程。

**修复建议**：阶段1/2 的 run 调用前同样建立 WS 连接（run_id 由前端生成传给 params），把步骤卡片升级为实时状态。

---

## 三、轻微问题（P2）

### P2-1 阶段1 上传模式：未自动识别顶层 module / 生成 filelist

方案 RTL-003 的金牌标准是"filelist 自动生成"，当前上传只是把文件内容读进编辑器（[stage1/page.tsx:81-84](frontend/src/app/stage1/page.tsx#L81-L84)），无顶层模块识别、无多文件 filelist。

### P2-2 收敛历史图内容不符合方案

方案 3.4 要求 Recharts 折线图展示 **WNS / 面积 / DRC 的多轮收敛趋势**；当前 [stage3/page.tsx:217-248](frontend/src/app/stage3/page.tsx#L217-L248) 是手绘 SVG 的"成功/失败"二值折线。后端 [api.py:528-545](server/api.py#L528-L545) 其实已聚合了 `metrics.wns/area/power/drc`，前端没用。修复：直接用 Recharts 画 metrics 趋势。

### P2-3 Agent Decision 引擎的跳过规则是"装饰性"的

实测 compose 返回的 `skipped: ["upf_check","dft_insert","rdc_check","cdc_check","atpg","low_power_check"]`——这些名字在 `TRUNCATION` 的任何场景步骤列表中都不存在，跳过规则从未真正裁剪过任何步骤（[agent_engine.py:26-33](server/agent_engine.py#L26-L33) 的 skip 名字与 [agent_engine.py:11-21](server/agent_engine.py#L11-L21) 的步骤名对不上）。方案 6.2 的 Sheet 1 能力池（~55 项原子能力）在代码中没有数据化实现。

### P2-4 Sheet 4（收敛循环）定义但从未执行

[agent_engine.py:137-161](server/agent_engine.py#L137-L161) 的 `STUCK_SIGNALS` / `STOP_LOSS` 是纯数据，没有任何代码消费它们。方案 6.3 的"活动 2 收敛循环"（诊断→回溯→重拼装→重跑→止损）完全未实现。当前 Flow 跑一遍就结束，无闸门检查、无跨阶段回退。

> ✅ **已修复 (2026-08-16)**：新增 [server/convergence.py](server/convergence.py)（诊断→决策树→回溯重拼装→重跑→止损，深度映射轮数 quick=1/standard=3/signoff=5），[api.py](server/api.py) 在 flow/run 后触发活动 2，stage3 前端展示每轮诊断/动作/指标。实测 500MHz gcd：WNS -1.138 → -0.638 → -0.013 逐轮改善（降频真实生效）。

### P2-5 DesignProfile.from_code 未接入

`api_compose` 使用默认 `DesignProfile()`（[api.py:108](server/api.py#L108)），设计特征（时钟域数/纯组合/门数）从未从 RTL 提取，`SKIP_RULES` 依赖的特征全部是默认值 → 单时钟设计不会跳 CDC、纯组合设计不会跳 CTS（与 P0-2 同源修复）。

### P2-6 阶段2 与方案 3.3 的功能差距

| 方案要求 | 现状 |
|---|---|
| 仿真控制面板（时间范围、断点、步进） | ❌ 只有采样点数滑块 |
| 覆盖率热力图（Line/Branch/FSM/Toggle） | ✅ 四项全覆盖（[adapter/coverage_runner.py](adapter/coverage_runner.py)）：Line/Toggle/Branch 来自 Verilator --coverage 臂级行点，FSM 来自同一次仿真的 VCD 状态寄存器取值域（如 q 到达 6/16 状态=37.5%）；Monaco 行级热力图绿/红标注，激励与自动激励仿真同源 |
| Bug 追踪面板（错误→源码行定位→分类） | ⚠️ 简化为问题清单（从 lint/编译结果生成 3 类条目），无源码定位 |
| SVA 属性模板引导 | ✅ 已实现（模板优先 + LLM 兜底） |
| 源码-波形联动（RTL-007 金牌标准） | ⚠️ 部分实现：覆盖率行级标注（源码↔执行证据联动）已落地；波形时间轴↔源码行映射仍无 |

### P2-7 右侧辅助面板与方案 3.5 不符

方案要求右栏固定四区：知识卡片（上下文切换）/ 通关进度（9 徽章）/ Agent 反馈流 / 操作历史。当前 [layout.tsx](frontend/src/app/layout.tsx) 的右栏只有"文件 / Agent / 信息"三个 tab，且首页实测"信息" tab 报 `next/dynamic` SSR bailout（dev 模式警告）。方案中"9 徽章通关进度"整个没有。

> ✅ **已修复 (2026-08-16)**：右栏新增 🏅 徽章 tab（9 徽章 3×3，铜/银/金对齐方案 6.2 能力池通过标准，阶段1/2/3 运行成功后自动颁发，localStorage 持久化）；信息 tab 增加知识卡片区（[lib/knowledge.ts](frontend/src/lib/knowledge.ts) 9 张，按当前阶段上下文切换）。
> ✅ **四区补齐 (2026-08-18)**：操作历史（[lib/oplog.ts](frontend/src/lib/oplog.ts)，各阶段运行/徽章解锁/归档自动记录，信息 tab 展示）；Agent 反馈流修复（原订阅 global 频道但后端从未广播 → push_ws 双广播，新增收敛循环/归档事件渲染，实时事件流+清空按钮）。至此方案 3.5 四区全部落地。

### P2-8 其他代码质量问题

| 位置 | 问题 |
|---|---|
| [api.py:524](server/api.py#L524) | autosave 每次往 `workspace_files["autosave"]` append，重复文件不去重，列表无限增长 |
| [chat.py:39](server/chat.py#L39) | LLM 调用超时 30s，deepseek-reasoner 推理模型实测 17s+，复杂问题容易超时；超时后 reply 变成"(AI 调用失败: ...)"且**仍被加入 history** |
| [icarus_runner.py:74](adapter/icarus_runner.py#L74) | `assertions_ok = "FAIL" not in stdout and "Error" not in stdout` 字符串匹配，信号名叫 `error` 或 `$display("No Error")` 都会误判 |
| [icarus_runner.py:78](adapter/icarus_runner.py#L78) | `re.search(r"(\d+)\s*ns|#(\d+)", stdout)` 若行首是 `#10` 则 group(1) 为 None → sim_time 报 "unknown" |
| [stage2/page.tsx:68](frontend/src/app/stage2/page.tsx#L68) | `svaCode.includes('`ifdef FORMAL') ? wrapper : wrapper` 三元两个分支相同，死代码 |
| [api.py:155-158](server/api.py#L155-L158) | 同步端点内用 `asyncio.run(ws_manager.broadcast(...))` 每个事件创建一个事件循环，效率低且在高并发下有隐患，建议改为后台线程任务 |
| [api.py:249](server/api.py#L249) | `idrc_drc` 步骤调用 `IEDARunner({"flows": ["routing"]})` —— 跑的又是 routing 而不是 DRC |
| 部署 | ChipMATE 本地 vLLM 模式未部署（8001/8002 无服务），当前全部走 DeepSeek API；方案 2.1 的离线备选（Qwen3 GGUF）未配置 |

---

## 四、建议修复顺序

| 优先级 | 问题 | 预估工作量 |
|---|---|---|
| 1 | P0-1 ACTION 协议（SYSTEM_PROMPT 补协议 + 前端兜底按钮） | 0.5 天 |
| 2 | P0-2 + P2-5 DesignProfile 接入（compose 分析 RTL → top_module 全链路） | 0.5 天 |
| 3 | P0-4 路由顺序（2 行移动） | 分钟级 |
| 4 | P0-3 + P1-5 对比实验接真实 Flow + Recharts | 2~3 天 |
| 5 | P1-1 / P1-2 GDS 与 iEDA 输出归属本次 run + 前端状态区分 | 1 天 |
| 6 | P1-3 SVA 模板保护 | 0.5 天 |
| 7 | P1-4 鉴权 + 路径白名单 | 1 天 |
| 8 | P1-6 WS 接入阶段1/2 | 0.5 天 |
| 9 | P2 其余（上传识别、收敛历史图、覆盖率、知识卡片等） | 视排期 |
